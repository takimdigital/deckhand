# LLM_CONTEXT — Deckhand v2.0.0
<!-- GENERATED by scripts/llm_context.py from 214 files (78 code) · fingerprint ace632f323dd. Do not edit: change the code or scripts/llm_context.core.md, then `python3 scripts/llm_context.py`. -->

READ: curated §0–§6 (~9k tokens) fully, in order. §7–§15 are generated lookup tables: jump by the INDEX line ranges or grep; don't read them through.

You are a model with no memory of this repo. This file is the map. Open a source file only to change it, or when a
table here points you at one. Lookup tables are grep-friendly: `^#### tryon/lib/engine.mjs`, `^- NO_CANDIDATES`,
`^- dh pool`. `sym@path:line` means symbol `sym` is defined at that line. Line numbers are exact for the fingerprint
above. After editing code, run `python3 scripts/llm_context.py` again.

## INDEX (section: lines in this file)
- §0 BOOT [cur]: L29-L53
- §1 ARCHITECTURE [cur]: L55-L83
- §2 STATE — every file the skill reads or writes outside its own folder [cur]: L85-L119
- §3 INVARIANTS — never break these (and what enforces each) [cur]: L121-L158
- §4 FLOWS (→ = then; each step names its code) [cur]: L160-L371
- §5 ROUTING — "to change X, edit Y (and prove it in Z)" [cur]: L373-L407
- §6 DEV — working on this repo [cur]: L409-L448
- §7 MAP [gen] — every file: L450-L648
- §8 COMMANDS [gen] — dh, tryon, helper API → handlers: L650-L758
- §9 MODULES [gen] — every code file: L760-L1793
- §10 ERRORS [gen] — code → where raised: L1795-L1890
- §11 ENV [gen] — variables read: L1892-L1930
- §12 DATA [gen] — shipped data + templates: L1932-L1973
- §13 DOCS [gen] — markdown outline: L1975-L2014
- §14 TESTS [gen] — what proves what: L2016-L2162
- §15 CI + HOOKS [gen]: L2164-L2176

## §0 BOOT [cur]

WHAT  Deckhand v2 is an agent skill (MIT, repo takimdigital/deckhand-skill; one skill folder SKILL.md).
      It lets an AI agent in any harness (Claude Code, Codex, Cursor, Hermes, OpenCode, Gemini CLI) take a non-technical owner
      from an idea to a live, owned web business: define → research → plan → build → brand → try-on → review → deploy →
      operate. It runs on their own $5 VPS with Coolify, or on a $0 Oracle track.
WHY   Cheap in tokens and verifiable. Scripts do every deterministic step. The model writes only words (copy,
      research, plans) and app-specific logic. Every "done" is a command's output.
HOW   Two engines plus data plus docs:
      - dh — Python 3.9+ stdlib, dhlib via dh.py. The control plane: phase state machine, gates, checks,
        build, brand, verify, deploy, learning, reuse. Entry: `python3 skills/deckhand/dh.py <cmd>` (`py` on Windows).
      - tryon — Node 18+, zero deps, tryon. Live component swap/tune/theme on the owner's running dev site, and
        `compose` for scratch pages. Entry: `node skills/deckhand/tryon/cli.mjs <cmd>`, or `dh tryon <cmd>`.
      - data: policy and catalogs. references: one short page per phase, loaded one at a time.
        templates: files copied into projects. ops/scripts: Coolify/Hostinger/backup clients, ported from v1 and
        live-proven.
PATHS A path starting with dhlib/ tryon/ data/ references/ templates/ ops/ tests/ dh.py SKILL.md is under skills/deckhand/.
      Every other path is from the repo root (scripts/ docs/ .github/ README.md AGENTS.md …).
      "<project>" = the owner's app folder; "~/.deckhand" = the owner's home state (DECKHAND_HOME overrides).
YOU ARE EITHER
  (a) USING the skill for an owner → run `dh next` and do exactly what it prints: one instruction, one reference to read,
      the lessons that apply. The owner-facing rules are SKILL.md (register, invariants,
      command surface).
  (b) WORKING ON this repo → the DEV section. Tests are offline. Every behaviour change ships with a test. Regenerate this file
      (a stale file fails CI).

## §1 ARCHITECTURE [cur]

```
owner ⇄ agent (any harness) ──shell──▶ dh (dhlib/*.py)                        one JSON object per call, exit 0|1|2
                                        │ run state  <project>/.deckhand/run.json   (state machine: phases + gates G1-G4)
                                        │ checks     dhlib/checks.py (a phase is done only when its script passes)
                                        │ subprocess ─▶ node tryon/compose.mjs (dh compose) · node tryon/cli.mjs (dh tryon)
                                        │ subprocess ─▶ git · npm/pnpm/bun/yarn · ops/scripts/coolify_api.py (dh deploy)
                                        ▼
owner's browser ─▶ tryon serve (tryon/server.mjs, 127.0.0.1:3999) ─ reverse proxy + injected overlay.js ─▶ dev server
                    /__dh/api/* (token) ─▶ tryon/lib/engine|draft|tune|sitetheme ─▶ writes the project's source
                    dev server HMR re-renders; the agent is NOT in this loop (0 model calls per click)
registries (https, cached in ~/.deckhand/cache/tryon) ─▶ materialize ─▶ <components>/dh-tryon/<slug>/ (staging)
```
- Contract. dh: emit@dhlib/util.py:70 prints one JSON object. Failures raise DhError@dhlib/util.py:77(code, message,
  **extra), and main@dhlib/cli.py:294 prints {ok:false, code, message, …} with exit 1. Exit 2 = argparse usage.
  tryon: `out()` in tryon/cli.mjs; failures throw TryonError@tryon/lib/engine.mjs:29(code, message, extra).
- Project detection. dh: project_root@dhlib/util.py:123 (--project, else the nearest .deckhand/run.json, else
  package.json). tryon: detectProject@tryon/lib/project.mjs:33 measures framework (next|vite), aliases, ui/lib dirs,
  package manager, deps, Tailwind major, globals.css, semantic tokens, and the primitive base (radix|base|any).
- Source edits (tryon). Parse with vendored @babel/parser (parse@tryon/lib/ast.cjs:22). Every node carries exact offsets,
  so every edit is a string splice on the original bytes. The result is re-parsed before it is written. Every open
  session has a backup, so discard is byte-exact.
- DOM → source. stamp@tryon/lib/stamp.cjs:33 adds `data-dh="file:line:col"` to every JSX element. This happens only in
  dev, through tryon/loader.cjs (webpack/Turbopack), tryon/vite.mjs, and the next-config wrapper written by
  setup@tryon/lib/setup.mjs:130.
- Learning. `dh …` and `dh run -- …` append to <project>/.deckhand/runs.jsonl (log_run@dhlib/learn.py:115).
  autopsy@dhlib/autopsy.py:596 turns that log, or a Claude Code transcript, into lessons/playbooks/proposals.
  `dh next` and `dh run` read them back.

## §2 STATE — every file the skill reads or writes outside its own folder [cur]

| path | written by | read by | holds |
|---|---|---|---|
| <project>/.deckhand/run.json | save@dhlib/state.py:46 | load@dhlib/state.py:39 | name, mode phased/auto, path pool/mine/existing/scratch, phases{status}, gates{status}, base |
| .deckhand/history.jsonl | log@dhlib/state.py:51 | humans | init / phase_done / gate / reopen events |
| .deckhand/brief.json | cmd_brief@dhlib/cli.py:31 | checks, pool.query, brand, sitelinks, harvest | business, shape, languages, audience, brand{name,tagline,primary,logo,social} (templates/brief.json) |
| .deckhand/research.json | the agent | research@dhlib/checks.py:23 | 3 competitors with URL, strengths and gaps; audience; conversion plays; features.now (templates/research.json) |
| .deckhand/sitemap.json | the agent, after init@dhlib/plan.py:26 | lint@dhlib/plan.py:62, render, split, sitelinks, verify routes | pages, actions with typed targets, nav, forms, features, entities (templates/sitemap.json) |
| .deckhand/PLAN.md · work/WP-*.md · blackboard.jsonl | render@dhlib/plan.py:207 · split@dhlib/plan.py:247 · bb_post@dhlib/plan.py:313 | owner · sub-agents | the plan for G1; one work package per bounded context; contract/done/blocker posts |
| .deckhand/copy.json | the agent | tryon/compose.mjs | section copy (schema in compose.mjs header; footer.columns/social, navbar.links) |
| .deckhand/dev.json · dev.log | dev_start@dhlib/build.py:227 | checks.build, verify | url, pid, port · the dev server log |
| .deckhand/swap-map.json | scan@dhlib/swap.py:59 | check@dhlib/swap.py:79 | vendor SDKs found → owned target |
| .deckhand/demo-copy.json | recordDemoCopy@tryon/lib/engine.mjs:763 | check@dhlib/brand.py:130 | demo words a kept design still shows (block), or AI-written words to confirm (ai:true → warn) |
| .deckhand/verify.json · VERIFY.md | run_verify@dhlib/verify.py:43 | checks.review, handoff, harvest | rows {check, ok, blocking, detail} |
| .deckhand/deploy.json | target@dhlib/deploy.py:37 · ship | checks.deploy, handoff, ops | app_uuid, url, last{commit,result}, smoke |
| .deckhand/runs.jsonl · failures.jsonl | log_run@dhlib/learn.py:115 · run_cmd@dhlib/learn.py:123 | load_runs@dhlib/autopsy.py:119, from_failure@dhlib/learn.py:167 | every command {cmd, exit, out} · failed tails |
| .deckhand/lessons.jsonl | add@dhlib/learn.py:61 (scope=project) | match@dhlib/learn.py:89 | project-only lessons |
| .deckhand/autopsy/<A-id>.md,.json | autopsy@dhlib/autopsy.py:596 | owner, `--apply` | the deterministic report |
| .deckhand/tryon/setup.json · runtime/ | setup@tryon/lib/setup.mjs:130 | unsetup@tryon/lib/setup.mjs:169, doctor | journal of the config patch (byte-exact undo) · copied loader/plugin |
| .deckhand/tryon/sessions/<id>.json · backups/<id>/ | open@tryon/lib/engine.mjs:195 | show/keep/discard, checks.tryon, verify | open→kept/discarded, variants[], shaBefore/After · original file bytes |
| .deckhand/tryon/drafts/<D>.json · <D>/draft.tsx | requestDraft@tryon/lib/draft.mjs:67 · the agent | checkDraft@tryon/lib/draft.mjs:163, completeDraft | AI draft brief + state pending/rejected/done · the agent's component |
| .deckhand/tryon/tune/<id>.json(+.orig) | tuneOpen@tryon/lib/tune.mjs:172 | tuneSet/Keep/Reset | dials, original file bytes and sha (FILE_CHANGED guard) |
| .deckhand/tryon/theme/last.json | themeApply@tryon/lib/sitetheme.mjs:236 | themeUndo@tryon/lib/sitetheme.mjs:259 | the files before the last Site apply |
| <project>/PENDING.md · HANDOFF.md | init@dhlib/state.py:55 (from templates/PENDING.md) · write@dhlib/handoff.py:11 | owner | facts only the owner holds · access LOCATIONS, commands, pending |
| <project>/NOTICE · THIRD_PARTY_NOTICES.md | clone@dhlib/build.py:75 · recordNotice@tryon/lib/engine.mjs:777 | never rewritten | upstream + licence of the base and of every kept design |
| <project>/<components>/sections/ or ui-kit/<slug>/ · dh-tryon/ | keep · open | the app | kept designs · staging (removed on keep/discard/clean) |
| <project>/globals.css marked blocks | tokenLayer@tryon/lib/theme.mjs:123 (deckhand:tokens) · open@tryon/lib/engine.mjs:195 (dh:css per variant) · themeCss@tryon/lib/sitetheme.mjs:96 (dh:theme) | the app | derived tokens · variant CSS · Site knobs |
| ~/.deckhand/profile.json · vault.env (0600) | set_fields@dhlib/profile.py:75 · vault_set@dhlib/profile.py:107 | secret@dhlib/profile.py:127 (env first, then vault) | owner facts · secrets (COOLIFY_TOKEN, GITHUB_TOKEN, …), never printed |
| ~/.deckhand/pool.json · bases/<name>/ | add_local@dhlib/pool.py:251 · harvest@dhlib/harvest.py:28 | rows@dhlib/pool.py:37 (ranked first) | personal bases (source mine) |
| ~/.deckhand/lessons.jsonl · playbooks.jsonl · autopsy/proposals/E-*.md | add@dhlib/learn.py:61 · apply_report@dhlib/autopsy.py:629 | match/preflight · _playbook@dhlib/guide.py:55 · a human | global lessons (+recipe, auto) · steps that finished a phase ≥2× · skill-fix proposals |
| ~/.deckhand/library/ (components/, components.index.json) | saveToLibrary@tryon/lib/library.mjs:50 | loadCatalog@tryon/lib/catalog.mjs:19 (r=mine, ranked first) | the owner's saved components |
| ~/.deckhand/registries.json · catalog/registry-<id>.json | addRegistry@tryon/lib/vet.mjs:89 | loadCatalog@tryon/lib/catalog.mjs:19 | vetted extra registries and their mapped items |
| ~/.deckhand/cache/tryon/ | getText@tryon/lib/registry.mjs:31 | same | fetched registry files (a click never refetches) |
| GitHub <login>/deckhand-base-<name> (private) · <login>/deckhand-library | harvest@dhlib/harvest.py:28 via push@dhlib/github.py:82 · library_upsert@dhlib/harvest.py:146 | library_sync@dhlib/harvest.py:159, clone@dhlib/build.py:75 | a harvested base · library.json + README table |

## §3 INVARIANTS — never break these (and what enforces each) [cur]

1. Output contract: one JSON object on stdout; exit 0 ok · 1 check failed/refused · 2 usage. Nothing else on stdout
   (emit@dhlib/util.py:70; tryon `out`). The one exception is `tryon serve`, which streams JSON lines, including
   {"event":"draft_request"}.
2. Dependencies: Python stdlib only (3.9+). Node built-ins only (18+). The only vendored code is tryon/vendor
   (babel parser, MIT). Never use the project's own `typescript` package: TS7 has no JS API (lesson S-008).
3. Deterministic: same inputs → same bytes. This covers ranking (rank@tryon/lib/catalog.mjs:43, score@dhlib/pool.py:50),
   verdicts (verdict@dhlib/vet.py:46, vetRegistry@tryon/lib/vet.mjs:31), autopsy reports, tune/theme transforms and
   this file. No clocks or randomness in outputs, except ids and timestamps in state.
4. Edits are exact and reversible. Every source edit is AST-located, re-parsed before it is written, and backed up.
   Discard, tune reset, theme undo and `tryon clean` restore bytes exactly (discard@tryon/lib/engine.mjs:857,
   tuneReset@tryon/lib/tune.mjs:214, themeUndo@tryon/lib/sitetheme.mjs:259, unsetup@tryon/lib/setup.mjs:169).
5. Dev tooling never ships. Stamps load only when NODE_ENV=development. Verify rows `prod-clean` and
   `tryon-closed` block (run_verify@dhlib/verify.py:43). `tryon clean` runs before review.
6. Owner facts are never invented. Unknowns go to PENDING.md. Demo copy left in a kept design blocks
   `dh rebrand check`. AI-written words are listed and warn until the owner confirms them
   (check@dhlib/brand.py:130, demoTexts@tryon/lib/engine.mjs:731, literalFit@tryon/lib/engine.mjs:706).
7. Secrets stay in the vault or env. They never appear on a command line, in a remote URL, stdout, a commit or
   HANDOFF.md. The GitHub token reaches git through GIT_CONFIG_* env (git_env@dhlib/github.py:71).
   `harvest --push` refuses on any secret-shaped file or string (secret_scan@dhlib/harvest.py:111, SECRETS_IN_BASE).
8. One licence policy (data/licenses.json) for bases, registries and the catalog. NOTICE and
   THIRD_PARTY_NOTICES.md are never deleted or rewritten (NEVER@dhlib/brand.py:18). AI-generated variants carry a
   provenance header and no third-party notice.
9. Nothing enters on trust: `dh pool vet|add` (verdict@dhlib/vet.py:46) and `tryon registry vet|add`
   (vetRegistry@tryon/lib/vet.mjs:31). A refused item is refused with the reason and what would be accepted
   (acceptable_text@dhlib/vet.py:34).
10. Gates are hard in phased mode. phase_done@dhlib/state.py:105 refuses out of order (OUT_OF_ORDER) or behind an
   unpassed gate (GATE_BLOCKED). A phase is done only when run@dhlib/checks.py:111 says ok (or `--force REASON`,
   which is recorded).
11. The autopsy never edits the skill. It writes lessons, playbooks, and proposals for a human or a reviewed PR.
12. The agent is never in the click loop. The overlay calls the local helper; the helper calls the engine. A new
   try-on feature must not require polling the model.
13. Every behaviour change ships with a test. A fix that lives only in prose isn't a fix. After any change,
   regenerate LLM_CONTEXT.md.
14. Windows parity. Use `py`; read files with CRLF normalized; delete trees with rmtree@dhlib/util.py:88 (read-only
   git objects); resolve launchers with which@dhlib/util.py:83 (npm.cmd). Line endings follow .gitattributes.
   CI runs Ubuntu and Windows.

## §4 FLOWS (→ = then; each step names its code) [cur]

F1 RUN LOOP
- `dh init` → init@dhlib/state.py:55 writes run.json and PENDING.md.
- `dh next` → next_step@dhlib/guide.py:65 returns:
  - STEPS[phase], a list of commands (STEPS@dhlib/guide.py:16);
  - `read`, the one reference to load;
  - `lessons` from preflight@dhlib/learn.py:100;
  - `worked_before` from playbooks;
  - `pool_top` during plan.
- Agent works → `dh phase done X` → run@dhlib/checks.py:111:
  - red → CHECK_FAILED with `why[]`;
  - green → in phased mode a gate may block → owner says go → `dh gate pass Gn`.
- `dh reopen X` resets X and every later phase and gate.
- Phases and their gates: PHASES@dhlib/state.py:15. G1 plan, G2 build, G3 tryon, G4 review.

F2 BUILD PATHS (run.json.path)
- pool/mine: `dh pool query` (query@dhlib/pool.py:112: score@dhlib/pool.py:50 = shape +40, features, freshness…;
  blockers exclude) → `dh clone NAME --to DIR` (clone@dhlib/build.py:75):
  - licence re-checked;
  - shallow clone at the measured commit, or a private GitHub clone for mine;
  - fresh git history (_fresh_git@dhlib/build.py:31);
  - NOTICE written; .env seeded with LOCAL secrets only (seed_env@dhlib/build.py:38);
  - base recorded.
- existing: `dh adopt PATH|URL` (adopt@dhlib/build.py:115); history kept.
- scratch: `dh scaffold` (scaffold@dhlib/build.py:135: create-next-app + templates/scaffold tokens/cn/Button) →
  `dh compose` (F3).
- Then:
  - `dh swap scan|check` swaps rented vendor SDKs for owned targets (TARGETS@dhlib/swap.py:14; payment processors
    are kept);
  - `dh dev start` (serve@dhlib/build.py:188: detached process group, waits until the URL answers < 500) → owner
    tests → G2.

F3 COMPOSE (scratch landing page, no codegen)
- compose@dhlib/build.py:157 → `node tryon/compose.mjs` reads .deckhand/copy.json → for each section:
  1. rank@tryon/lib/catalog.mjs:43 (with a kit-coherence bonus);
  2. fetch and materialize;
  3. parameterize@tryon/lib/transplant.mjs:600, then bind@tryon/lib/transplant.mjs:800 with the copy;
  4. fillLinks@tryon/lib/sitelinks.mjs:118 for footer/navbar;
  5. bake@tryon/lib/engine.mjs:497.
- Output: components/sections/<slug>/, and the page imports them in order.
- Rules: one <h1> per page; copy with no room in the design is reported, never dropped silently.

F4 TRY-ON SESSION WIRING
- `tryon setup` (setup@tryon/lib/setup.mjs:130):
  - next.config → `withDeckhandTryon(config)` (patchNextConfig@tryon/lib/setup.mjs:87);
  - vite → dhTryon() (patchViteConfig@tryon/lib/setup.mjs:110);
  - adds the token layer (ensureTokens@tryon/lib/engine.mjs:158);
  - journaled.
- Restart dev → `tryon serve` (startServer@tryon/server.mjs:62):
  - proxy rewrites Host/Origin to localhost (Next dev guard) and injects the overlay into HTML;
  - HMR websockets are passed through;
  - API gated by the per-run token; writes run one at a time.
- Overlay (tryon/overlay.js): owner clicks Try-on → picks an element (nearest data-dh stamp, breadcrumb of
  ancestors) → chooses a slot (tryon/lib/slots.mjs) → tabs "Swap it | Tune it"; a "Site" button sits on the pill.

F5 OPEN (swap) open@tryon/lib/engine.mjs:195
1. Find the element at file:line:col.
2. extractUnits@tryon/lib/transplant.mjs:522 gets the owner's content units: text, links, images, inputs, and
   unrolled .map lists.
3. rank@tryon/lib/catalog.mjs:43 ranks candidates: mine first; primitive-base mismatch hidden and counted.
4. For each candidate:
   1. fetchBundle@tryon/lib/materialize.mjs:133: tryon/lib/registry.mjs json or gh transport, cached, https only,
      size/file caps.
   2. writeBundle@tryon/lib/materialize.mjs:234:
      - every file needed; the project's ui primitives reused; cn from lib/utils;
      - palette classes → tokens (normalizeClasses@tryon/lib/theme.mjs:68); compat fixes.
   3. Blocks only:
      - parameterize@tryon/lib/transplant.mjs:600: static units → `content.x ?? demo`, demo lists → list slots;
        chrome, demo logos and forms hidden;
      - then fillLinks@tryon/lib/sitelinks.mjs:118, then bind@tryon/lib/transplant.mjs:800;
      - fit gate: a candidate carrying < half the owner's units is skipped as POOR_FIT.
5. One batched npm install.
6. Sort by fit.
7. Write ONE wrapper (`data-dh-session`, variant 0 = the original, hidden). Imports are marked `// dh-tryon:<id>`.
   The result is re-parsed; the backup is saved.
8. Cycling ← → is client-side display toggling: zero writes. show@tryon/lib/engine.mjs:388 persists the choice.
9. Nothing fits → NO_CANDIDATES or NO_VARIANTS with `draft:{file,line,col,slot}`; the overlay offers an
   AI draft (F7).

F6 KEEP / DISCARD
- keep@tryon/lib/engine.mjs:790:
  - the wrapper collapses to the chosen variant under a clean local name;
  - the folder graduates to <components>/sections/ or ui-kit/<slug>;
  - bake@tryon/lib/engine.mjs:497 writes the literal copy into the component (show/hide switches resolved);
  - losers and their CSS are deleted;
  - THIRD_PARTY_NOTICES entry (recordNotice@tryon/lib/engine.mjs:777; not for AI drafts);
  - leftover demo copy → demo-copy.json.
- Keeping variant 0 is a discard.
- discard@tryon/lib/engine.mjs:857: backup restore when sha matches, else a surgical unwrap.
- `tryon save` → saveToLibrary@tryon/lib/library.mjs:50: refuses without a licence or when a credential-shaped string
  is found.

F7 AI DRAFT (labelled fallback; the agent writes once, scripts gate)
1. Overlay "AI draft", or `tryon draft` → requestDraft@tryon/lib/draft.mjs:67 writes drafts/<D>.json. The brief holds:
   - content units, markup, hrefs, lists, images, dynamic expressions;
   - the original JSX;
   - the project's ui/utils/packages;
   - the rules and `write_to`.
   `tryon serve` also prints {"event":"draft_request"} on stdout.
2. Agent: `tryon drafts [--wait]` → writes <D>/draft.tsx (default export) → `tryon draft-done --id D`.
3. checkDraft@tryon/lib/draft.mjs:163 gates:
   - DRAFT_EMPTY, DRAFT_FILE, DRAFT_ENTRY, DRAFT_PARSE, DRAFT_EXPORT;
   - DRAFT_IMPORT: only the project's ui/utils, installed packages, draft-local files;
   - DRAFT_SIDE_EFFECT: no fetch/env;
   - DRAFT_RAW_COLOR: tokens only;
   - DRAFT_IMAGE / DRAFT_LINK: only the owner's;
   - DRAFT_DROPPED_CONTENT: literalFit@tryon/lib/engine.mjs:706 must carry every owner unit.
4. Gate outcome:
   - rejected → DRAFT_REJECTED with problems; state rejected; fix and re-run;
   - passed → completeDraft@tryon/lib/draft.mjs:241 folds any open session on the same element (discard, reopen
     with [aiItem, ...its registry items]) → engine.open with `candidates`.
5. Every AI variant is labelled everywhere:
   - `generated:true`; purple "AI-generated" badge;
   - file header "AI-generated … no licence notice applies";
   - invented words listed as ai-copy (warn).

F8 TUNE (one element; the Impeccable verbs as deterministic knobs)
- tuneOpen@tryon/lib/tune.mjs:172 saves the original, then tuneSet@tryon/lib/tune.mjs:188.
- Each set recomputes from the ORIGINAL: tuneElement@tryon/lib/tune.mjs:125 → tuneClassList@tryon/lib/tune.mjs:112 →
  tuneToken@tryon/lib/tune.mjs:54 over the subtree's class strings. The result is written each time; HMR shows
  exactly what would be kept.
- Knobs: DIALS@tryon/lib/tune.mjs:34. Presets: quieter/bolder/airy/compact/clarity/softer/sharper
  (PRESETS@tryon/lib/tune.mjs:25).
- Keep = stop tracking. Reset = byte-exact. FILE_CHANGED if the file was edited meanwhile.

F9 SITE (whole look)
- Knobs (KNOBS@tryon/lib/sitetheme.mjs:36): accent (ACCENTS@tryon/lib/sitetheme.mjs:22 or any hex), neutrals,
  corners, density (Tailwind 4 --spacing), headlines (--text-*), fonts (FONTS@tryon/lib/sitetheme.mjs:27).
- Preview: the overlay asks `/__dh/api/theme-vars` for the exact values (themeVars@tryon/lib/sitetheme.mjs:74) and sets
  them on :root.
- Apply (themeApply@tryon/lib/sitetheme.mjs:236):
  - writes one `dh:theme` block at the end of globals.css (themeCss@tryon/lib/sitetheme.mjs:96; unlayered body
    rule, headings in @layer base);
  - fonts: rewriteFonts@tryon/lib/sitetheme.mjs:155 rewrites next/font in the root layout (AST) and adds
    --font-heading;
  - backs up to theme/last.json.
- Undo is byte-exact.

F10 REVIEW `dh verify` → run_verify@dhlib/verify.py:43
- Rows: typecheck · lint (advisory) · build · prod-clean · tryon-closed · secrets · leaks/honesty
  (check@dhlib/brand.py:130) · routes · a11y (advisory) · audit (advisory).
- routes: every planned static route plus every internal home-page link must answer < 400, on the production build
  served on a free port 4100+ (falls back to --url or the dev server).
- Writes verify.json and VERIFY.md. Any red blocking row → VERIFY_FAILED → G4 can't pass.

F11 DEPLOY
- First time: runbooks references/ops/00-user-checklist.md → 10/11 (server) → 20/21 (domain) → 30 (app).
- `dh deploy target --app UUID --url U` → `dh deploy ship` (ship@dhlib/deploy.py:68):
  1. clean tree required (DIRTY_TREE);
  2. git push;
  3. Coolify /deploy;
  4. wait_for_deploy@ops/scripts/coolify_api.py:106 with expect_commit=HEAD, so a stale deployment can't pass;
  5. smoke@dhlib/deploy.py:47 (http → NO_TLS warning; sslip/nip → PREVIEW_URL warning).
- `dh handoff` (write@dhlib/handoff.py:11).

F12 LEARN
- `dh run -- CMD` (run_cmd@dhlib/learn.py:123):
  - failure → match@dhlib/learn.py:89 against lessons (seed data/lessons.seed.jsonl + ~/.deckhand + project) →
    prints the known fix;
  - `--fix` replays an `auto` recipe (safe commands only), then retries once.
- `dh autopsy [src|--latest] [--apply]` (autopsy@dhlib/autopsy.py:596):
  1. Sources: Claude JSONL (load_claude@dhlib/autopsy.py:75), runs.jsonl (load_runs@dhlib/autopsy.py:119), plain.
  2. Failure = non-zero exit, or error MARKERS in an executing command's output (pipes hide exits;
     is_failure@dhlib/autopsy.py:308). Designed refusals (USAGE, GATE_BLOCKED, OUT_OF_ORDER, DRAFT_REJECTED) are
     not failures.
  3. Episodes are merged by signature@dhlib/autopsy.py:343. The recipe = the state-changing steps between the LAST
     failed attempt and the success (verification commands excluded).
  4. Owner (_owner@dhlib/autopsy.py:375): skill · environment · project.
  5. Rung (_rung@dhlib/autopsy.py:389): eliminate > preflight > reorder > gate > pitfall.
  6. `--apply` (apply_report@dhlib/autopsy.py:629): lessons with recipe/auto, skill proposals E-*.md, and playbooks
     that `dh next` shows as `worked_before` (seen ≥ 2).
  7. In a dev session (cwd has skills/deckhand/SKILL.md), everything is owned by the skill and no playbooks are made.

F13 VET
- Bases: `dh pool vet|add owner/repo`:
  - measure@dhlib/pool.py:184 reads the GitHub API and raw files; no clone, no code run;
  - evidence@dhlib/vet.py:100 → verdict@dhlib/vet.py:46;
  - hard criteria: licence, consistent, alive, runnable, clean, free; soft criteria are warnings and ranking
    penalties;
  - add refuses with REFUSED.
- Registries: `tryon registry vet|add` → vetRegistry@tryon/lib/vet.mjs:31:
  - checks: refused-list hosts, the repo licence, shadcn schema, usable items, sampled downloads with no paywall;
  - accepted → addRegistry@tryon/lib/vet.mjs:89, items mapped by mapShadcnItems@tryon/lib/regmap.mjs:8.

F14 REUSE
- `dh harvest --name N [--push]` (harvest@dhlib/harvest.py:28):
  1. copy tracked files, minus DROP@dhlib/harvest.py:25;
  2. neutralize the brand to N;
  3. write a manifest (proven by verify and live URL);
  4. register in the personal pool.
- `--push`:
  1. secret_scan@dhlib/harvest.py:111;
  2. ensure_repo@dhlib/github.py:57 creates a private <login>/deckhand-base-N;
  3. push to main;
  4. library_upsert@dhlib/harvest.py:146 updates <login>/deckhand-library (library.json + README table).
- Another machine: `dh pool sync` (library_sync@dhlib/harvest.py:159) → `dh clone N` (private clone on the recorded
  branch).
- Components: `tryon save` → ~/.deckhand/library (ranked first).

F15 OPERATE
- `dh ops suggest` (suggest@dhlib/ops.py:20: data/bots.json vs the brief and whether it's deployed) →
  `dh ops add BOT --runner github|cron` (add@dhlib/ops.py:44):
  - generic bots are real scripts (templates/bots: watchdog, link_audit, notify);
  - app bots are a spec plus skeleton the agent completes.
- Change pipeline: references/80-operate.md.

F16 BRAND
- `dh rebrand scan|apply|check` (apply@dhlib/brand.py:72): package name, metadata, template names, primary colour
  token, monogram icon.
- check blocks on: template names, demo companies, lorem, example contacts, placeholder images, demo-copy markers,
  third-party logos as social proof.

## §5 ROUTING — "to change X, edit Y (and prove it in Z)" [cur]

| change | edit | prove in |
|---|---|---|
| a phase, its order, gate or reference | PHASES@dhlib/state.py:15, dhlib/checks.py, STEPS@dhlib/guide.py:16, references/NN-*.md | tests/test_dh.py |
| what `dh next` prints | next_step@dhlib/guide.py:65 | test_dh.py |
| a dh command or flag | build_parser@dhlib/cli.py:47 + dispatch@dhlib/cli.py:104 + the module; SKILL.md §4 table | test_dh.py |
| plan lint rules (E0–E11, W1–W4) | lint@dhlib/plan.py:62 | test_dh.py |
| pool ranking / vetting criteria / licence policy | score@dhlib/pool.py:50 · verdict@dhlib/vet.py:46 · data/licenses.json | tests/test_vet.py |
| clone/adopt/scaffold/dev server | dhlib/build.py | test_dh.py |
| rebrand / honesty findings | check@dhlib/brand.py:130 | test_dh.py |
| verify rows | run_verify@dhlib/verify.py:43 | test_dh.py (Verify) |
| deploy / smoke / Coolify client | dhlib/deploy.py · ops/scripts/coolify_api.py | ops/tests/test_coolify_api.py, test_dh.py (Deploy) |
| server, DNS, backup and storage clients (used by the ops runbooks) | ops/scripts/hostinger_api.py · ops/scripts/coolify_backup_setup.py · ops/scripts/backup_verify.py · ops/scripts/b2_setup.py · ops/scripts/tigris_bucket.py · ops/scripts/repo_presence.py | ops/tests (pytest) |
| lessons, `dh run`, recipes | dhlib/learn.py · seed data/lessons.seed.jsonl | test_dh.py, test_autopsy.py |
| autopsy detection / owner / rung | is_failure@dhlib/autopsy.py:308 · _owner@dhlib/autopsy.py:375 · _rung@dhlib/autopsy.py:389 · MARKERS | tests/test_autopsy.py |
| harvest / GitHub library | dhlib/harvest.py · dhlib/github.py | tests/test_library.py (mock API) |
| candidate ranking / slots | rank@tryon/lib/catalog.mjs:43 · tryon/lib/slots.mjs | tryon/test/engine.test.mjs |
| fetching registry items | tryon/lib/registry.mjs (fixtures: DH_FIXTURES, record: DH_RECORD) | engine.test.mjs |
| staging files / import rewriting | tryon/lib/materialize.mjs | engine.test.mjs |
| owner content carried into designs | tryon/lib/transplant.mjs | tryon/test/transplant.test.mjs, sections.test.mjs |
| footer/navbar links from the plan | tryon/lib/sitelinks.mjs | tryon/test/sections.test.mjs |
| colours → tokens | tryon/lib/theme.mjs | setup-theme-server.test.mjs |
| open/show/keep/discard/bake | tryon/lib/engine.mjs | engine.test.mjs, sections.test.mjs |
| AI draft gates | checkDraft@tryon/lib/draft.mjs:163 | tryon/test/draft.test.mjs |
| Tune knobs / presets | tryon/lib/tune.mjs (+ overlay tuneUI) | tryon/test/tune.test.mjs |
| Site knobs / fonts | tryon/lib/sitetheme.mjs (+ overlay sitePanel) | tune.test.mjs |
| overlay UI | tryon/overlay.js (plain ES5-ish browser JS, no build) | browser e2e (manual, Playwright + Chromium) |
| helper routes / proxy | tryon/server.mjs | tryon/test/setup-theme-server.test.mjs |
| dev-only wiring (Next/Vite) | tryon/lib/setup.mjs · tryon/lib/stamp.cjs · tryon/loader.cjs · tryon/vite.mjs | setup-theme-server.test.mjs, stamp.test.mjs |
| registry vetting | tryon/lib/vet.mjs · data/registries.json | tryon/test/vet.test.mjs |
| the shipped catalog | tryon/catalog-build.mjs (maintainer, network) → data/components.index.json | engine.test.mjs |
| installer | install.sh · install.ps1 | manual |
| owner-facing docs | README.md · docs/USE-CASES.md · SKILL.md | scripts/version_check.py (versions) |
| this file | code (generated part) · scripts/llm_context.core.md (curated part) · scripts/llm_context.py | scripts/test_llm_context.py |

## §6 DEV — working on this repo [cur]

- Tests (all offline):
  - `python3 -m unittest discover -s skills/deckhand/tests`
  - `python3 -m pytest skills/deckhand/ops/tests -q`
  - `node --test skills/deckhand/tryon/test/*.test.mjs`
  - `python3 -m unittest discover -s scripts -p 'test_*.py'`
- Registry HTTP comes from tryon/test/fixtures/registry (DH_FIXTURES; re-record with DH_RECORD).
  GitHub is a local mock API plus file:// remotes (DH_GITHUB_API/DH_GITHUB_GIT).
  The fixture site is tryon/test/fixtures/site.
- CI: .github/workflows/ci.yml runs every suite on Ubuntu and Windows (Windows non-blocking), plus the version
  check, plus `llm_context.py --check` off main.
  .github/workflows/llm-context.yml regenerates this file on main when it drifts.
- Hooks: `git config core.hooksPath .githooks` once per clone. .githooks/pre-commit then regenerates this file
  from the index on every commit.
- Release: RELEASING.md.
  - scripts/version_check.py: README badge = SKILL.md version = CHANGELOG top.
  - scripts/leak_sweep.py: private terms list, refuses to pass without it.
- Style:
  - terse docstrings/headers that state the contract (they are copied into MODULES, so write them for a model);
  - JSON-first outputs; error codes UPPER_SNAKE;
  - one reason per refusal, plus what would be accepted.
- Traps already paid for (do not re-learn them):
  - Killing by pattern (`pkill -f X`, `ps | grep X | xargs kill`) kills your own shell when X is in your command
    line (exit 144). Kill by PID.
  - Regex `(?:\s+.*\n)+` backtracks catastrophically (\s matches \n). Use `[ \t]+`.
  - Python heredoc patch scripts collide with `"""` quoting. Write the patch to a file, then run it.
  - A GITHUB_TOKEN in the env overrides the vault (secret@dhlib/profile.py:127 reads env first). Tests must pop it.
  - A bare repo's HEAD may be master. Push and clone explicit branches (main).
  - Empty dirs are not tracked by git (a fixture's public/). ensurePlaceholder@tryon/lib/engine.mjs:37 creates
    public/.
  - Windows read-only .git objects make shutil.rmtree fail → rmtree@dhlib/util.py:88.
  - Next dev blocks HMR via a proxy unless Host/Origin = localhost (S-006). Turbopack rules must not use `as` (S-005).
  - create-next-app's `body{font-family:Arial}` beats next/font variables. The dh:theme body rule is unlayered for
    that reason.
  - Live registry/GitHub/Google Fonts calls may be blocked in sandboxes. Prove behaviour with fixtures and say what
    wasn't live-tested.
  - LLM_CONTEXT.md merge conflict → never hand-merge. Run `python3 scripts/llm_context.py` and commit the result.
- Not proven live yet: a full `dh deploy ship` against a real server (the Coolify client itself is live-proven from
  v1), and the $0 Oracle track. Business bots are specs plus skeletons.

## §7 MAP [gen] — every file

`path  lines  purpose` — directories in order; a collapsed dir is one line.

[./ (repo root)]
  .gitattributes  9  Line-ending law for this repo (PUBLIC). Windows checkouts default to CRLF — a CRLF .sh dies on the VPS (sheba…
  .gitignore  5
  AGENTS.md  22  AGENTS.md — Deckhand
  CHANGELOG.md  97  Changelog
  LICENSE  21
  README.md  181  Deckhand
  RELEASING.md  8  Releasing
  install.ps1  37  Deckhand installer (Windows) — copies skills\deckhand into every agent harness found and adds a dh.cmd shim.…
  install.sh  58  Deckhand installer — copies (or links) skills/deckhand into every agent harness it finds, and puts a `dh` shi…
[.githooks/]
  pre-commit  17  Regenerates LLM_CONTEXT.md from what is being committed (the git index) and stages it: every commit carries a…
[.github/]
  FUNDING.yml  9  Sponsor button shown on the repo page — https://github.com/sponsors Docs: https://docs.github.com/repositorie…
[.github/workflows/]
  ci.yml  51  Deckhand v2 CI — every suite is offline (registry responses are recorded fixtures).
  llm-context.yml  41  Keeps LLM_CONTEXT.md (the cold-start map any AI reads first) in step with main. Branches and pull requests ar…
[assets/]
  logo.svg  16
  preview.html  59
  render-social-preview.ps1  113
  social-preview.png  bin  (binary)
[docs/]
  USE-CASES.md  214  Use cases — copy a sentence, get a result
[scripts/]
  leak_sweep.py  245  leak_sweep.py — the release gate for this (public) repo.
  llm_context.core.md  418  BOOT
  llm_context.py  999  llm_context.py — builds LLM_CONTEXT.md: this whole repo, compressed for a model that has no memory.
  test_llm_context.py  118  llm_context.py — the cold-start map must be exact, deterministic and unable to rot silently (stdlib unittest).
  version_check.py  42  version_check.py — every human-readable version claim must match: README badge == skills/deckhand/SKILL.md `v…
[skills/deckhand/ (skill root — the paths below are relative to it)]
  SKILL.md  134  Deckhand v2 — operating specification
  dh.py  12  Deckhand control plane entry point: `python3 dh.py <command>` (Windows: `py dh.py <command>`). Stdlib only, P…
[data/]
  bots.json  38  keys: version, note, bots — generic bots ship as working scripts; app bots ship as a spec + skeleton the agen…
  components.index.json  1  keys: version, built, count, items
  lessons.seed.jsonl  20
  licenses.json  34  keys: _about, accepted, refused, package_license_refused, paid_packages, paywall_text, secret_files — One lic…
  pool.json  743  keys: version, policy, count, templates — MIT/Apache-2.0 only; every row measured from the live repository (d…
  registries.json  86  keys: version, updated, policy, refused, registries — MIT / Apache-2.0 only. A registry is indexed only after…
data/pool/  (12 files)  measured detail of each pool row (dh pool show NAME); rows are in data/pool.json
[dhlib/]
  __init__.py  0
  autopsy.py  679  AUTOPSY: a session in, evidence-based improvements out — deterministic, no model, no opinion.
  brand.py  177  REBRAND: the owner's identity in, the template's out — deterministic, then PROVED by a leak check.
  build.py  258  BUILD: four ways in, one result — a project folder that runs locally, with its own git history.
  checks.py  112  Phase checks — each returns {"ok": bool, "why": [...], "hint": str}. A gate that cannot fail is not a gate.
  cli.py  313  dh — the Deckhand control plane. One JSON object per command on stdout; exit 0 = ok, 1 = check failed / refus…
  deploy.py  102  DEPLOY: ship to Coolify and PROVE it — terminal deployment for YOUR commit + a live smoke check.
  github.py  113  The owner's GitHub, used as their personal library: private base repos + one index repo.
  guide.py  92  `dh next` — ONE instruction for the current state: what to run, which single reference to load, the lessons t…
  handoff.py  63  HANDOFF.md — everything the owner (or the next agent) needs, with secret LOCATIONS, never values.
  harvest.py  171  HARVEST: a finished business becomes the owner's next head start — in THEIR GitHub library.
  learn.py  200  LEARN: a failure is paid for once.
  ops.py  100  OPERATE: bots that run the business while the owner sleeps — suggested from what was built.
  plan.py  327  The plan as an interaction graph (.deckhand/sitemap.json) — linted, rendered, split for parallel agents.
  pool.py  257  The boilerplate pool — query it, never read it whole.
  profile.py  176  The owner, once: ~/.deckhand/profile.json (facts) + ~/.deckhand/vault.env (secrets, chmod 600).
  state.py  165  The run state machine: one file (.deckhand/run.json) says where a build is and what comes next.
  swap.py  98  SWAP: rented services out, owned ones in — proved by absence (the SDK) and presence (the target).
  util.py  178  Shared plumbing for the dh control plane (stdlib only, Python 3.9+).
  verify.py  172  REVIEW: one command, evidence rows, a red row blocks the deploy gate.
  vet.py  113  VET: nothing enters the pool on trust — every base is measured, then judged against written criteria.
[ops/scripts/]
  b2_setup.py  97  b2_setup.py — B2 bucket + lifecycle + scoped app key via the B2 Native API. LIVE-VERIFIED 2026-09-20.
  backup_verify.py  137  backup_verify.py — the honest proof for Coolify backups (ref 55 §3): LIST THE BUCKETS.
  coolify_api.py  467  vps-ops — thin Coolify REST client (stdlib only).
  coolify_backup_setup.py  96  coolify_backup_setup.py — Coolify S3 storages + DB backup schedules (ref 55 §0/§1). LIVE-VERIFIED.
  hostinger_api.py  262  vps-ops — thin Hostinger API client (stdlib only).
  repo_presence.py  232  repo_presence.py — render an app's public face from ONE filled JSON.
  tigris_bucket.py  82  tigris_bucket.py — create a Tigris bucket the SAFE way and PROVE it serves reads.
[ops/tests/]
  test_backup_verify.py  55  Cold-read audit controls for backup_verify.py: the gate must fail on an empty schedule list and on a failed n…
  test_coolify_api.py  257
  test_hostinger_api.py  91
  test_repo_presence.py  106  Tests for repo_presence.py — render from one JSON, fail loudly, never leave a half-README.
[references/]
  00-define.md  37  00 — DEFINE (the only interview)
  10-research.md  30  10 — RESEARCH (evidence, not opinions)
  20-plan.md  39  20 — PLAN (the page map is an interaction graph)
  30-build.md  63  30 — BUILD (four ways in, one running app)
  40-brand.md  20  40 — BRAND (their identity in, the template's out)
  50-tryon.md  98  50 — TRY-ON (live section/component swap, zero model calls per click)
  60-review.md  26  60 — REVIEW (one command, evidence rows)
  70-deploy.md  32  70 — DEPLOY (proof, not hope)
  80-operate.md  44  80 — OPERATE (the business keeps running; the pack keeps learning)
[references/ops/]
  00-user-checklist.md  169  00 — User checklist: the only things we ask the user for
  10-bootstrap-vps.md  273  10 — Bootstrap: SSH key → key attach → firewall → Coolify → token → hardening → snapshot
  11-oracle-free-tier.md  136  11 — Oracle Cloud Always Free: the $0 preview server (Arm)
  12-provider-price-sheet.md  64  12 — Provider price sheet (Oracle out? pick from here)
  20-domain-dns-ssl.md  162  20 — Domain, DNS & SSL: records → Hostinger API (or manual) → Coolify domain → Let's Encrypt
  21-free-domain-cloudflare.md  112  21 — Preview domains: .pp.ua (free) or an owned domain → Cloudflare DNS-only
  30-deploy-app.md  255  30 — Deploy an app (repo → Coolify app → envs → Postgres → HTTPS → smoke)
  40-change-pipeline.md  206  40 — The change pipeline (implement → push → deploy → wait → smoke → report)
  50-ops-monitoring.md  138  50 — Operate: status, logs, metrics, cleanup, backups, updates, incidents
  55-offsite-backups.md  134  55 — Offsite backups (never on the same VPS)
  56-app-object-storage.md  59  56 — App object storage (uploads) — RustFS via Coolify
  60-migrate-to-paid.md  103  60 — Migrate: free preview → paid host (Coolify → Coolify)
  70-auth-and-email.md  142  70 — Auth & login email (verify + reset that actually arrives)
  80-mcp-integrations.md  61  80 — MCP integrations (modular catalog)
[templates/]
  PENDING.md  8  PENDING — {{NAME}}
  brief.json  16  keys: name, business, shape, audience, market, languages, brand, features
  research.json  17  keys: business_summary, audience, competitors, conversion, discovery, features, unproven, sources
  sitemap.json  51  keys: name, nav, pages, features, forms, entities
  work-package.md  37  {{ID}} — {{TITLE}}
[templates/bots/]
  link_audit.py  70  link_audit.py — weekly crawl: broken internal links, pages without <title>/meta description, missing robots.t…
  notify.py  54  notify.py — one message, any channel (stdlib). Channel from env: TELEGRAM_BOT_TOKEN+TELEGRAM_CHAT_ID, DISCORD…
  watchdog.py  111  watchdog.py — is the business up? (stdlib; runs anywhere: GitHub Actions, the VPS cron, a laptop)
  workflow.yml  25  deckhand bot: {{ID}} — {{TITLE}} Verify the schedule by its own trigger once: `gh workflow run deckhand-{{ID}…
[templates/ops/]
  OPS.md  112  OPS.md — <App name> (`<domain>`)
  oci-cloud-init.yaml  25  cloud-config vps-ops — Oracle Cloud (Ubuntu) first-boot prep for a Coolify host. Paste into: Create Instance…
[templates/ops/mail-router/]
  .env.example  22  --- chain ---------------------------------------------------------------
  README.md  37  mail-router — multi-provider email failover (drop-in)
  schema.sql  25  mail-router schema — run once in the app's Postgres (or port to Drizzle migrations).
  send.ts  120  lib/email/send.ts — the ONLY send path in the app (auth + product mail). Multi-provider failover across free…
[templates/ops/repo-presence/]
  LICENSE.proprietary.txt  7
  README.md  33  repo-presence — the one-time "nice repo" pass (ref 40 §6b)
  README.template.md  57  {{NAME}}
  repo.example.json  38  keys: repo, name, tagline, live_url, version, intro, features, stack
[templates/ops/vps-backup/]
  README.md  69  vps-backup — the deep layer (restic → B2 + Tigris) — LIVE-VERIFIED 2026-09-20
  coolify-backup.sh  110  vps-ops deep backup layer (ref 55 §5) — restic → TWO offsite S3 providers (B2 + Tigris). LIVE-VERIFIED 2026-0…
  fix-clock.cmd  11  Deckhand - Windows clock fix for S3 SigV4 skew (vps-ops ref 55 section 7). Run ELEVATED on the machine that p…
  local-pull.sh  68  Deckhand home pull (vps-ops ref 55 §7) — runs on the USER'S machine, NOT the VPS. v3 (2026-09-20): fast-fail…
  run-pull.cmd  13  Deckhand home pull - Windows Task Scheduler wrapper (vps-ops ref 55 section 7). Copy this file where local-pu…
  rustfs-local.compose.yml  31  RustFS local — the "home S3" target for the backup pipeline (vps-ops ref 55 §7). Docker Desktop (or any Docke…
[templates/scaffold/app/]
  globals.css  83  radius: 0.625rem; background: oklch(1 0 0); foreground: oklch(0.145 0 0); card: oklch(1 0 0); card-foreground…
[templates/scaffold/components/ui/]
  button.tsx  64
[templates/scaffold/lib/]
  utils.ts  6
[tests/]
  test_autopsy.py  181  dh autopsy — deterministic session analysis (stdlib unittest).
  test_dh.py  337  dh control-plane tests — stdlib unittest: `python3 -m unittest discover -s skills/deckhand/tests`.
  test_library.py  151  Personal boilerplate library on the owner's GitHub — offline: a mock GitHub API + file:// git remotes.
  test_vet.py  94  dh pool vet/add — written criteria, evidence in, verdict out (offline: rows are built, not fetched).
[tryon/]
  catalog-build.mjs  149  catalog-build.mjs — (re)build data/components.index.json from the licensed registries. Maintainer tool; owner…
  cli.mjs  157  tryon — Deckhand's live component swap. One JSON object per command on stdout; exit 0 = ok.
  compose.mjs  162  compose.mjs — a page from licensed blocks + the plan's copy, no code generation.
  loader.cjs  24  Deckhand try-on stamp loader — webpack AND Turbopack (Next.js `turbopack.rules`). Dev-only twice over: `setup…
  overlay.js  714  Deckhand try-on overlay — injected by the local helper into the owner's dev site (never prod). Pick any eleme…
  server.mjs  207  server.mjs — the try-on helper. ONE local process, no agent polling, no LLM per click:
  vite.mjs  29  Deckhand try-on — Vite plugin (dev server only). Usage in vite.config: import dhTryon from '<skill>/tryon/vit…
[tryon/lib/]
  ast.cjs  133  ast.cjs — source-exact parsing for try-on.
  catalog.mjs  84  catalog.mjs — deterministic candidate ranking. Same inputs -> same order, always. Personal library items (r =…
  draft.mjs  282  draft.mjs — the AI fallback for try-on.
  engine.mjs  913  engine.mjs — one try-on session, end to end, with no LLM in the loop.
  library.mjs  74  library.mjs — the owner's personal component pool ("mine"), shared by try-on and `dh`.
  materialize.mjs  326  materialize.mjs — turn a catalog item into files inside the project, self-contained:
  project.mjs  117  project.mjs — a measured profile of the target project (never assumed): framework, aliases, where ui/lib live…
  registry.mjs  147  registry.mjs — fetch a catalog item into a normalized, self-contained bundle: { files: [{ key, path, content…
  regmap.mjs  37  regmap.mjs — one mapping from a shadcn-schema registry index to catalog items, used by the maintainer build (…
  setup.mjs  199  setup.mjs — put the dev-only stamp into a project, and take it out again byte-exact.
  sitelinks.mjs  179  sitelinks.mjs — a footer or navbar shows the site's own routes, never the design's demo menu.
  sitetheme.mjs  267  sitetheme.mjs — the whole site's look in six knobs, previewed instantly, applied exactly.
  slots.mjs  66  slots.mjs — the one slot vocabulary shared by the catalog, the overlay and the planner. A slot is WHAT a thin…
  stamp.cjs  68  stamp.cjs — the dev-only source map from DOM to JSX.
  theme.mjs  183  theme.mjs — make a registry component wear the SITE's colours, deterministically.
  transplant.mjs  952  transplant.mjs — "their design, your words", deterministically (no model call).
  tune.mjs  225  tune.mjs — adjust a picked element's design with knobs, not prompts (zero model calls).
  vet.mjs  117  vet.mjs — a component registry enters try-on only after it is measured against written criteria.
[tryon/test/]
  draft.test.mjs  175
  engine.test.mjs  108
  helpers.mjs  39
  sections.test.mjs  130
  setup-theme-server.test.mjs  116
  stamp.test.mjs  52
  transplant.test.mjs  151
  tune.test.mjs  125
  vet.test.mjs  66
tryon/test/fixtures/registry/  (40 files)  recorded registry HTTP responses, replayed offline via DH_FIXTURES
[tryon/test/fixtures/site/]
  next.config.ts  7
  package.json  7  keys: name, private, scripts, dependencies, devDependencies
  tsconfig.json  34  keys: compilerOptions, include, exclude
[tryon/test/fixtures/site/app/]
  globals.css  26  background: #ffffff; foreground: #171717;
  page.tsx  34
[tryon/test/fixtures/site/components/]
  hero.tsx  23
[tryon/test/fixtures/site/components/ui/]
  button.tsx  63
[tryon/test/fixtures/site/lib/]
  utils.ts  6
tryon/vendor/  (2 files)  vendored @babel/parser (MIT) — never edit, never parse

## §8 COMMANDS [gen] — dh, tryon, helper API → handlers

### dh (python3 skills/deckhand/dh.py …) — parser: dhlib/cli.py build_parser; dispatch: dhlib/cli.py dispatch
`command args → handlers@file:line`. Global: --project DIR (default: nearest dir with .deckhand/run.json, else package.json). Output: one JSON object; exit 0 ok · 1 check failed/refused ({ok:false,code,message,…}) · 2 usage.

- dh init --name NAME [--mode phased|auto=phased] [--path pool|mine|existing|scratch=pool]
    → dispatch L107: state.init@55
- dh status
    → dispatch L109: state.load@39, state.summary@98
- dh next
    → dispatch L112: guide.next_step@65
- dh brief {show|set} [PAIRS…]
    → dispatch L115: cmd_brief@dhlib/cli.py:31
- dh phase {done|skip} {define|research|plan|build|brand|tryon|review|deploy|operate} [--reason REASON] [--force FORCE]
    → dispatch L117: state.phase_done@105, state.phase_skip@131
    --force: record done despite a red check (the reason is kept and shown)
- dh gate {pass} GATE [--note NOTE]
    → dispatch L124: state.gate_pass@144
- dh reopen {define|research|plan|build|brand|tryon|review|deploy|operate} --reason REASON
    → dispatch L126: state.reopen@154
- dh profile {show|set|doctor} [PAIRS…] [--offline]
    → dispatch L128: profile.set_fields@75, profile.doctor@142, profile.load@51, profile.profile_path@43
- dh vault {set|list} [NAME]
    → dispatch L135: profile.vault_set@107, profile.vault_read@93, profile.vault_path@47
- dh pool {query|show|vet|add|list|sync} [TARGET] [--shape SHAPE] [--features FEATURES] [--languages LANGUAGES] [--top TOP=3] [--mine] [--lane LANE=web]
    → dispatch L142: pool.query@112, pool.show@125, harvest.library_sync@159, pool.vet@224, pool.add@230, pool.rows@37
- dh plan {init|lint|render|split} [--agents AGENTS=3] [--force]
    → dispatch L163: plan.split@247, plan.init@26, plan.lint@62, plan.render@207
- dh bb {post|read} [--wp WP=all] [--kind KIND=note] [--msg MSG] [--last LAST=40]
    → dispatch L175: plan.bb_post@313, plan.bb_read@321
- dh clone TEMPLATE --to TO [--no-install]
    → dispatch L184: build.clone@75
- dh adopt SOURCE [--to TO] [--install]
    → dispatch L186: build.adopt@115
- dh scaffold --to TO [--pm npm|pnpm|bun|yarn=npm]
    → dispatch L188: build.scaffold@135
- dh compose [--page PAGE=app/page.tsx] [--sections SECTIONS=hero,features,pricing,faq,cta,footer] [--copy COPY]
    → dispatch L190: build.compose@157
- dh dev {start|stop|status} [--port PORT]
    → dispatch L182: build.dev_start@227, build.dev_stop@244, build.dev_status@256
- dh rebrand {scan|apply|check} [PAIRS…] [--dry] [--allow ALLOW]
    → dispatch L193: brand.check@130, brand.scan@51, brand.apply@72, _kv@dhlib/cli.py:21
- dh swap {scan|check}
    → dispatch L204: swap.check@79, swap.scan@59
- dh verify [--url URL] [--skip SKIP] [--allow ALLOW]
    → dispatch L212: verify.run_verify@43
- dh deploy {target|ship|smoke|raw} [-- rest…] [--app APP] [--url URL] [--force]
    → dispatch L218: deploy.target@37, deploy.ship@68, deploy.smoke@47, deploy.passthrough@98
- dh ops {suggest|add|list} [BOT] [--runner RUNNER=github]
    → dispatch L230: ops.add@44, ops.suggest@20, ops.catalog@16
- dh learn {add|match|preflight|promote|from-failure|list} [--phase PHASE=build] [--symptom SYMPTOM] [--cause CAUSE] [--fix FIX] [--signature SIGNATURE] [--command COMMAND] [--rung RUNG=pitfall] [--scope SCOPE=global] [--log LOG] [--text TEXT] [--stack STACK]
    → dispatch L239: learn.add@61, learn.promote@177, learn.from_failure@167, learn.all_lessons@31, learn.match@89, learn.preflight@100
- dh run [--phase PHASE] [--fix] [-- argv…]
    → dispatch L256: learn.run_cmd@123
    --fix: replay a proven auto-safe recipe, retry once
- dh autopsy [SOURCE] [--latest] [--apply]  — deterministic session analysis: failures -> recipes, lessons, skill proposals, playbooks
    → dispatch L263: autopsy.autopsy@596
    SOURCE: a Claude Code transcript .jsonl or a run log (default: .deckhand/runs.jsonl, else the latest transcript)
- dh harvest --name NAME [--to TO] [--repo REPO] [--public] [--push]
    → dispatch L266: harvest.harvest@28
    --push: secret scan, then a private repo on your GitHub + your library index
- dh handoff
    → dispatch L269: handoff.write@11
- dh tryon [-- rest…]  — passthrough to the try-on engine (node)

### tryon (node skills/deckhand/tryon/cli.mjs …, or `dh tryon …`) — switch in tryon/cli.mjs main
flags: --project DIR (default .). Output: one JSON line; exit 0 ok, 1 failed, 2 usage.

- tryon setup [--project .] wire the dev-only stamp (restart dev)  [cli.mjs:57] → setup.setup@130
- tryon serve [--project .] [--port 3999] [--target http://127.0.0.1:3000]  [cli.mjs:58] → server.detectTarget@44, server.startServer@62
- tryon doctor [--project .] is everything wired? (no writes)  [cli.mjs:66] → project.detectProject@33, setup.readJournal@69, server.detectTarget@44
- tryon slots [--project .] what can be tried here  [cli.mjs:82] → project.detectProject@33, catalog.slotsSummary@77, catalog.loadCatalog@19
- tryon query --slot hero [--project .] ranked candidates (no writes)  [cli.mjs:86] → project.detectProject@33, catalog.rank@43, catalog.loadCatalog@19
- tryon inspect --file f --line n --col c [--slot s]  [cli.mjs:93] → engine.inspect@894
- tryon try --file f --line n --col c --slot s [--count 4] [--no-install] [--registry r]  [cli.mjs:94] → engine.open@195
- tryon draft --file f --line n --col c --slot s [--note "…"] | --session S  [cli.mjs:99] → draft.requestDraft@67, draft.loadDraft@41
- tryon drafts [--wait [--timeout 1800]] pending requests (+ each brief path)  [cli.mjs:105] → draft.listDrafts@50, draft.publicDraft@58
- tryon draft-check --id D run the gates, write nothing  [cli.mjs:116] → draft.checkDraft@163
- tryon draft-done --id D gate + show it (labelled AI-generated)  [cli.mjs:117] → draft.completeDraft@241
- tryon registry vet|add --index https://…/registry.json --repo owner/name [--id x]  [cli.mjs:122] → vet.listRegistries@107, vet.removeRegistry@111, vet.vetRegistry@31, vet.addRegistry@89
- tryon show --id S --idx N make variant N the visible one  [cli.mjs:133] → engine.show@388
- tryon keep --id S [--idx N] collapse to N, bake the text, graduate  [cli.mjs:134] → engine.keep@790
- tryon discard --id S byte-exact restore  [cli.mjs:135] → engine.discard@857
- tryon save --id S [--name n] kept component -> personal library  [cli.mjs:136] → library.saveToLibrary@50
- tryon library  [cli.mjs:137] → library.listLibrary@62
- tryon status [--project .]  [cli.mjs:138] → engine.listSessions@62, setup.readJournal@69
- tryon clean [--project .] discard open sessions, unwire  [cli.mjs:143] → engine.listSessions@62, engine.discard@857, setup.unsetup@169

### helper HTTP API (tryon serve) — POST /__dh/api/<name>, header x-dh-token; switch in tryon/server.mjs api()
Other routes: GET /__dh/overlay.js (the UI) · GET /__dh/events?t=TOKEN (SSE progress) · everything else = reverse proxy to the dev server with the overlay <script> injected into HTML.

- state  [server.mjs:80] → project.detectProject@33, catalog.slotsSummary@77, catalog.loadCatalog@19, draft.listDrafts@50, engine.listSessions@62
- inspect  [server.mjs:87] → engine.inspect@894
- open  [server.mjs:88] → engine.open@195
- show  [server.mjs:89] → engine.show@388
- keep  [server.mjs:90] → engine.keep@790
- discard  [server.mjs:91] → engine.discard@857
- more  [server.mjs:92] → engine.loadSession@53, engine.discard@857, engine.open@195
- save  [server.mjs:97] → library.saveToLibrary@50
- draft  [server.mjs:98] → draft.requestDraft@67
- tune-open  [server.mjs:105] → tune.tuneOpen@172
- tune-set  [server.mjs:106] → tune.tuneSet@188
- tune-keep  [server.mjs:107] → tune.tuneKeep@205
- tune-reset  [server.mjs:108] → tune.tuneReset@214
- theme-state  [server.mjs:110] → sitetheme.themeState@224
- theme-vars  [server.mjs:111] → sitetheme.themeVars@74
- theme-apply  [server.mjs:112] → sitetheme.themeApply@236
- theme-undo  [server.mjs:113] → sitetheme.themeUndo@259
- draft-status  [server.mjs:114] → draft.loadDraft@41, engine.loadSession@53, engine.publicSession@365, draft.publicDraft@58

## §9 MODULES [gen] — every code file

Per code file: purpose (its own header, verbatim) · deps (internal imports) · used-by · api (public: name(params)@line — doc) · int (internal name@line) · const. Order: control plane, try-on, ops, templates, repo scripts.

#### dh.py  (12 lines, py)
  | Deckhand control plane entry point: `python3 dh.py <command>` (Windows: `py dh.py <command>`).
  | Stdlib only, Python 3.9+. Start with `dh.py next`.
#### dhlib/__init__.py  (0 lines, py)
#### dhlib/autopsy.py  (679 lines, py)
  | AUTOPSY: a session in, evidence-based improvements out — deterministic, no model, no opinion.
  | Sources (auto-detected):
  |   claude  Claude Code session JSONL (~/.claude/projects/<project-slug>/<session>.jsonl) — `--latest` finds it
  |   runs    deckhand's own run log (.deckhand/runs.jsonl): every `dh …` call and every `dh run -- …` — works the
  |           same in any harness (Hermes, Codex, Cursor…) as long as risky commands go through `dh run`
  |   plain   JSONL of {"cmd": "…", "exit": 0, "out": "…"} (or {"edit": "path"}) — anything can emit it
  | Pipeline:
  |   events → failures (a non-zero exit, OR error markers in the output of an executing command, because
  |   `cmd | tail` hides exit codes) → episodes (a failure signature, its retries, and everything until the same
  |   command family succeeded) → the working recipe (the state-changing commands and edited files between the
  |   LAST failed attempt and the success) → an owner (skill · environment · project; scratch and exploration are
  |   never episodes, tool errors are listed apart) → the
  |   strongest fix-ladder rung the evidence supports (eliminate > preflight > reorder > gate > pitfall).
  | Artifacts:
  |   .deckhand/autopsy/<id>.md|.json   the report — same input, same bytes
  |   --apply                           lessons WITH recipes into ~/.deckhand/lessons.jsonl (the owner's ledger:
  |                                     `dh run` prints the recipe the moment the error reappears, `--fix` replays
  |                                     the auto-safe ones), skill proposals into ~/.deckhand/autopsy/proposals/
  |                                     (repro + the fix that worked + the regression test it needs), and
  |                                     playbooks (successful phase runs) into ~/.deckhand/playbooks.jsonl
  |   The skill itself is never edited by the autopsy: a proposal is applied by a person or a reviewed PR.
  deps: dhlib/learn.py, dhlib/state.py, dhlib/util.py
  used-by: dhlib/cli.py
  api load_claude(path)@75
  api load_runs(path)@119
  api detect(path)@132
  api claude_dir_for(project)@149
  api latest_source(root)@153
  api segments(cmd)@192
  api family(seg, trusted=False)@227
  api families(cmd, trusted=False)@254
  api primary(cmd, trusted=False)@279 — The families that carry the command's meaning — empty for pure exploration, scratch or edit scripts.
  api edits_in(cmd)@288 — Files a command writes as an edit (cat > f, sed -i … f, python3 - <<EOF with p = 'f').
  api display(cmd, meta=None)@298
  api is_failure(ev)@308 — (failed, masked). grep/diff/test exit 1 is an answer, not a failure; pipes hide exit codes.
  api signature(ev)@343
  api sig_regex(sig)@363
  api analyze(events, meta, lessons=None)@408
  api playbooks(events, failed_idx, meta)@536 — Successful phase runs: the state-changing commands between two `dh phase done` successes.
  api render(rep, name)@558
  api autopsy(root, source=None, latest=False, apply=False)@596
  api apply_report(root, rep, rid)@629
  int: _text@67 _strip_heredocs@165 _subst@179 _tokens@215 _is_read_only@270 _aside@274 _scratch@284 _last_prog@303 _paths_to@333 _sid@369 _owner@375 _rung@389 _match@529 _proposal@664
  const: RUNG_ORDER@37 READ_ONLY@38 EXECUTING@43 MARKERS@44 SPECIFIC@48 FAILWORDS@49 CRASH_RX@50 ENV_RX@51 GENERATED_RX@53 CONFIG_RX@54 SAFE_RX@55 SKILL_RX@57 TEST_FILE_RX@58 TEST_RUNNER@59 VERIFY@60 EXIT_MEANING@61 SCRIPTED_EDIT@189
#### dhlib/brand.py  (177 lines, py)
  | REBRAND: the owner's identity in, the template's out — deterministic, then PROVED by a leak check.
  | apply: package name, metadata title/description, site config names, template display names in
  | source/docs, the primary colour token, a monogram icon when there is no logo. LICENSE / NOTICE /
  | THIRD_PARTY_NOTICES are never touched (they are the legal condition of reuse).
  | check: the honesty + leak gate — template names, demo companies, lorem ipsum, example contacts,
  | placeholder images, demo-copy markers, third-party brand logos presented as social proof; words an AI
  | try-on draft wrote (labelled for the owner) are a warning until confirmed or rewritten.
  deps: dhlib/state.py, dhlib/util.py
  used-by: dhlib/checks.py, dhlib/cli.py, dhlib/verify.py
  api scan(root)@51
  api apply(root, brand=None, dry=False)@72
  api check(root, allow=…)@130 — Findings with severity: block (fails the gate) | warn.
  int: _template_names@25 _iter_text@40 _hex_ok@68
  const: NEVER@18 DEMO_NAMES@19 BRAND_LOGO_FILES@22
#### dhlib/build.py  (258 lines, py)
  | BUILD: four ways in, one result — a project folder that runs locally, with its own git history.
  | clone <template>   a pool row (public or yours): shallow clone at the measured commit, fresh
  |                    history, NOTICE (upstream + commit + licence), .env seeded with LOCAL secrets only
  | adopt <path|url>   the owner's existing project, history kept
  | scaffold           from scratch: create-next-app + the canonical UI base (tokens, cn, Button)
  | compose            a landing page assembled from licensed blocks with the plan's copy (no LLM)
  | dev start|stop     run it and prove it answers
  deps: dhlib/github.py, dhlib/pool.py, dhlib/state.py, dhlib/util.py
  used-by: dhlib/cli.py, dhlib/verify.py
  api seed_env(dest)@38 — .env from .env.example; generate values ONLY for local secrets (never vendor keys).
  api install(dest)@59
  api clone(name, to, do_install=True)@75
  api adopt(src, to=None, do_install=False)@115
  api scaffold(to, pm='npm')@135
  api compose(root, page, sections, copy)@157
  api serve(root, script, port, log, wait=180)@188 — Start `<pm> run <script> --port N` detached (own process group); wait until it answers < 500.
  api kill_tree(pid)@220
  api dev_start(root, port=None, wait=180)@227
  api dev_stop(root)@244
  api dev_status(root)@256
  int: _fresh_git@31 _record_base@66 _free_port@172 _answers@180
  const: LOCAL_SECRET_KEYS@28
#### dhlib/checks.py  (112 lines, py)
  | Phase checks — each returns {"ok": bool, "why": [...], "hint": str}. A gate that cannot fail is not a gate.
  deps: dhlib/brand.py, dhlib/plan.py, dhlib/util.py
  used-by: dhlib/state.py
  api define(root, s)@17
  api research(root, s)@23
  api plan(root, s)@43
  api build(root, s)@49
  api brand(root, s)@71
  api tryon(root, s)@77
  api review(root, s)@88
  api deploy(root, s)@95
  api operate(root, s)@107
  api run(phase, root, s)@111
  int: _res@13
  const: REQUIRED_BRIEF@10
#### dhlib/cli.py  (313 lines, py)
  | dh — the Deckhand control plane. One JSON object per command on stdout; exit 0 = ok, 1 = check
  | failed / refused, 2 = usage. Run `dh next` whenever unsure: it prints the one next step.
  deps: dhlib/autopsy.py, dhlib/brand.py, dhlib/build.py, dhlib/deploy.py, dhlib/guide.py, dhlib/handoff.py, dhlib/harvest.py, dhlib/learn.py, dhlib/ops.py, dhlib/plan.py, dhlib/pool.py, dhlib/profile.py, dhlib/state.py, dhlib/swap.py, dhlib/util.py, dhlib/verify.py
  api cmd_brief(a)@31
  api build_parser()@47
  api dispatch(a)@104
  api main(argv=None)@294
  int: _root@17 _kv@21 _log@283
#### dhlib/deploy.py  (102 lines, py)
  | DEPLOY: ship to Coolify and PROVE it — terminal deployment for YOUR commit + a live smoke check.
  | First-time server/domain/app setup is a runbook (references/ops/00→30); from then on:
  |   dh deploy target --app <uuid> --url https://…   (once)
  |   dh deploy ship                                   (every release: push → deploy → wait → smoke)
  | Credentials come from the vault (COOLIFY_TOKEN) + profile (coolify.url); never from chat.
  deps: dhlib/profile.py, dhlib/util.py
  used-by: dhlib/cli.py
  api deploy_path(root)@33
  api target(root, app, url)@37
  api smoke(root, url=None, contains=None)@47
  api ship(root, force=False, timeout=900)@68
  api passthrough(args)@98 — `dh deploy raw <coolify_api args>` — the full client (logs, envs, envset, status, tunnel …).
  int: _creds@23
#### dhlib/github.py  (113 lines, py)
  | The owner's GitHub, used as their personal library: private base repos + one index repo.
  | Stdlib only. The token comes from the vault (`dh vault set GITHUB_TOKEN`, a fine-grained token with
  | "Administration: write" to create repos and "Contents: write" to push) and is handed to git through the
  | environment (GIT_CONFIG_* variables) — never on a command line, never in a remote URL, never on disk.
  | DH_GITHUB_API / DH_GITHUB_GIT point elsewhere for tests (a local mock API and file:// remotes).
  deps: dhlib/profile.py, dhlib/util.py
  used-by: dhlib/build.py, dhlib/harvest.py
  api token(required=True)@23
  api api(method, path, body=None, ok=…)@32
  api me()@50
  api ensure_repo(full, description='', private=True)@57 — Create owner/name if missing (private by default). Returns {created, empty}.
  api git_env()@71 — Auth for git over HTTPS, passed as environment config (not visible in `ps`, not stored).
  api remote(full)@78
  api push(dest, full, branch='main')@82
  api clone(full, to, branch=None)@90
  api get_file(full, path)@98
  api put_file(full, path, text, message, sha=None)@107
  const: API@19 GIT@20
#### dhlib/guide.py  (92 lines, py)
  | `dh next` — ONE instruction for the current state: what to run, which single reference to load,
  | the lessons that apply, and what makes the phase done. The agent never re-reads the whole skill.
  deps: dhlib/learn.py, dhlib/pool.py, dhlib/state.py, dhlib/util.py
  used-by: dhlib/cli.py
  api next_step(root)@65
  int: _playbook@55
  const: DH@13 TRYON@14 STEPS@16
#### dhlib/handoff.py  (63 lines, py)
  | HANDOFF.md — everything the owner (or the next agent) needs, with secret LOCATIONS, never values.
  deps: dhlib/profile.py, dhlib/util.py
  used-by: dhlib/cli.py
  api write(root)@11
#### dhlib/harvest.py  (171 lines, py)
  | HARVEST: a finished business becomes the owner's next head start — in THEIR GitHub library.
  |   dh harvest --name bakery-base [--push] [--repo owner/name] [--public]
  | The project's tracked files are copied (no .env, no .deckhand state, no logs), the owner's brand is
  | neutralised to the base's own name (so the NEXT project's rebrand replaces it and its leak check catches
  | leftovers), a manifest records what the base contains and what proved it (last verify, live URL), and the
  | base is registered in the personal pool — ranked first whenever it fits.
  | --push: a secret scan must be clean, then the base goes to a PRIVATE repo on the owner's GitHub
  | (owner/deckhand-base-<name>) and its row goes into their library index repo (owner/deckhand-library:
  | library.json + a readable README). Any machine, any harness: `dh pool sync` pulls the index, `dh pool query`
  | ranks it, `dh clone <name> --to dir` starts from it in one command.
  deps: dhlib/github.py, dhlib/pool.py, dhlib/profile.py, dhlib/util.py, dhlib/verify.py
  used-by: dhlib/cli.py
  api harvest(root, name, to=None, repo=None, private=True, push=False)@28
  api secret_scan(dest)@111
  api library_repo(login)@130
  api library_upsert(lib, row, private=True)@146
  api library_sync()@159 — Pull the owner's GitHub library index into the personal pool (any machine, any harness).
  int: _readme@134
  const: DROP@25 SECRET_FILE@108
#### dhlib/learn.py  (200 lines, py)
  | LEARN: a failure is paid for once.
  |   dh run -- <cmd>        run anything; on failure the output is matched against known lessons and the
  |                           proven fix is printed at once (no re-debugging), and the failure is recorded
  |   dh learn add           a fixed failure -> a lesson {signature regex, cause, fix, command, rung}
  |   dh learn from-failure  the same, with the signature derived from the last recorded failure
  |   dh learn preflight P   the lessons for phase P (+ this stack), max 8 lines — `dh next` includes them
  |   dh learn promote       lessons seen twice -> a patch proposal for the skill itself (fix ladder:
  |                           eliminate > preflight > reorder > gate > pitfall). Pitfalls are debt.
  | Global lessons live in ~/.deckhand/lessons.jsonl (every project benefits); project ones in .deckhand/.
  deps: dhlib/state.py, dhlib/util.py
  used-by: dhlib/autopsy.py, dhlib/cli.py, dhlib/guide.py
  api all_lessons(root=None)@31
  api add(root, phase, symptom, cause, fix, signature=None, command=None, rung='pitfall', scope='global', stack=None, extra=None)@61
  api match(root, text)@89
  api preflight(root, phase, limit=8)@100
  api log_run(root, cmd, code, out='', phase=None)@115 — Every command, success or not, in .deckhand/runs.jsonl — the harness-neutral source `dh autopsy` reads.
  api run_cmd(root, argv, phase=None, fix=False)@123
  api from_failure(root, fix, cause, rung='pitfall', scope='global', command=None)@167
  api promote(root)@177 — Lessons seen >= 2 become a patch proposal for the reference that owns their phase.
  int: _paths@24 _stack@44 _signature_from@157
  const: RUNGS@21
#### dhlib/ops.py  (100 lines, py)
  | OPERATE: bots that run the business while the owner sleeps — suggested from what was built.
  | `suggest` ranks data/bots.json against the brief (shape, features) and what is deployed.
  | `add <id> --runner github|cron` installs it: generic bots are real scripts (ops/bots/*.py, stdlib)
  | plus a schedule; app bots get a spec + skeleton the agent completes against THIS app's data model.
  | A schedule is verified only by its own trigger — the output says exactly how.
  deps: dhlib/util.py
  used-by: dhlib/cli.py
  api catalog()@16
  api suggest(root)@20
  api add(root, bot_id, runner='github')@44
#### dhlib/plan.py  (327 lines, py)
  | The plan as an interaction graph (.deckhand/sitemap.json) — linted, rendered, split for parallel agents.
  | Every clickable thing is an edge with a typed target:
  |   route:/path · anchor:#id · modal:<id> · api:METHOD /path · external:https://… · state:<page>.<state>
  |   · mailto:… · tel:… · download:<file>
  | `lint` proves the map has no dead end, no orphan page, no form without success AND error, no API
  | without an owning feature, no protected page without a way in. `split` cuts it into work packages
  | (one bounded context per feature + a shell) that agents build in parallel, coordinating through an
  | append-only blackboard instead of each other's context windows.
  deps: dhlib/util.py
  used-by: dhlib/checks.py, dhlib/cli.py
  api sitemap_path(root)@22
  api init(root, force=False)@26
  api lint(root, sm=None)@62
  api render(root)@207
  api split(root, agents=3)@247 — Work packages: one per feature (its pages, APIs, entities) + a shell (layout, nav, marketing).
  api bb_post(root, wp, kind, msg, refs=None)@313
  api bb_read(root, wp=None, kind=None, last=40)@321
  int: _target@40 _route_re@45 _actions@51
  const: KINDS@19 BB_KINDS@310
#### dhlib/pool.py  (257 lines, py)
  | The boilerplate pool — query it, never read it whole.
  | Public rows: data/pool.json (+ data/pool/<slug>.json measured details). Personal rows:
  | ~/.deckhand/pool.json (the owner's harvested bases, ranked first). Every row is MEASURED from the live
  | repository (`dh pool add owner/repo`: GitHub API + raw files, no clone, no code execution).
  | The ranking is a pure function of (brief, rows): same input, same order, reasons attached.
  deps: dhlib/profile.py, dhlib/util.py, dhlib/vet.py
  used-by: dhlib/build.py, dhlib/cli.py, dhlib/guide.py, dhlib/harvest.py
  api personal_path()@33
  api rows(include_personal=True)@37
  api score(row, brief)@50
  api query(brief, top=3)@112
  api show(name)@125
  api detect_stack(pkg, tree)@153
  api detect_features(pkg, tree, text)@172
  api measure(repo)@184
  api vet(repo, mine=False)@224 — Measure + judge, write nothing (`dh pool vet`).
  api add(repo, mine=False, shape=None, features=None, row=None)@230
  api add_local(path, meta)@251 — Register a harvested base (a folder or a private repo) in the personal pool.
  int: _days_since@43 _gh@135 _raw@145
  const: OK_LICENSES@20 OWNER_LICENSES@21 FEATURE_WORDS@22 VENDOR_PKGS@24
#### dhlib/profile.py  (176 lines, py)
  | The owner, once: ~/.deckhand/profile.json (facts) + ~/.deckhand/vault.env (secrets, chmod 600).
  | The profile answers the questions every project would otherwise ask again (providers, domain habits,
  | defaults, languages). Secrets never enter the profile, the chat, or a repo: scripts read the vault
  | directly, `dh vault list` shows names only, and `dh profile doctor` proves which capabilities are
  | reachable WITHOUT printing a value — "access before asks".
  deps: dhlib/util.py
  used-by: dhlib/cli.py, dhlib/deploy.py, dhlib/github.py, dhlib/handoff.py, dhlib/harvest.py, dhlib/pool.py
  api profile_path()@43
  api vault_path()@47
  api load()@51
  api set_fields(pairs)@75
  api vault_read()@93
  api vault_set(name, value=None)@107
  api secret(name)@127 — Env first (CI / harness secrets), then the vault. Never logged.
  api doctor(online=True)@142
  int: _set@55 _get@63 _probe@133
  const: FIELDS@19 SECRET_NAMES@37 SECRETISH@72
#### dhlib/state.py  (165 lines, py)
  | The run state machine: one file (.deckhand/run.json) says where a build is and what comes next.
  | Phases run in order. A phase is DONE only when its check (a script, never an opinion) passes.
  | In `phased` mode a gate after a phase blocks the next one until the owner says go
  | (`dh gate pass G1`); in `auto` mode gates pass themselves and are logged.
  | `dh next` prints ONE instruction + the ONE reference to load + the lessons for that phase:
  | the agent never needs to re-read the whole skill to know what to do.
  deps: dhlib/checks.py, dhlib/util.py
  used-by: dhlib/autopsy.py, dhlib/brand.py, dhlib/build.py, dhlib/cli.py, dhlib/guide.py, dhlib/learn.py
  api state_path(root)@35
  api load(root, required=True)@39
  api save(root, s)@46
  api log(root, event)@51
  api init(root, name, mode='phased', path='pool')@55
  api current(s)@76 — First phase that is neither done nor skipped (operate is the steady state).
  api blocking_gate(s)@85 — A gate owed by a finished phase that still blocks progress (phased mode only).
  api summary(root, s)@98
  api phase_done(root, phase, evidence=None, force_reason=None)@105 — Mark a phase done — only after its check passes (checks live in dhlib.checks).
  api phase_skip(root, phase, reason)@131
  api gate_pass(root, gate, note='')@144
  api reopen(root, phase, reason)@154 — Owner wants changes: reopen a phase (and everything after it).
  const: PHASES@15 PHASE_IDS@26 GATES@27 MODES@31 PATHS@32
#### dhlib/swap.py  (98 lines, py)
  | SWAP: rented services out, owned ones in — proved by absence (the SDK) and presence (the target).
  | scan  -> .deckhand/swap-map.json: every vendor SDK found (package.json + imports) with its target.
  | check -> exit 1 while a vendor SDK is still imported/installed, or a target is missing.
  | Payment processors are KEPT by default (a processor is a service, not a code lock-in).
  deps: dhlib/util.py
  used-by: dhlib/cli.py, dhlib/verify.py
  api scan(root)@59
  api check(root)@79
  int: _deps@39 _usages@44
  const: TARGETS@14 KEEP@36
#### dhlib/util.py  (178 lines, py)
  | Shared plumbing for the dh control plane (stdlib only, Python 3.9+).
  used-by: dhlib/autopsy.py, dhlib/brand.py, dhlib/build.py, dhlib/checks.py, dhlib/cli.py, dhlib/deploy.py, dhlib/github.py, dhlib/guide.py, dhlib/handoff.py, dhlib/harvest.py, dhlib/learn.py, dhlib/ops.py, dhlib/plan.py, dhlib/pool.py, dhlib/profile.py, dhlib/state.py, dhlib/swap.py, dhlib/verify.py, dhlib/vet.py
  api home()@20 — ~/.deckhand (DECKHAND_HOME overrides) — the owner's portable state.
  api now()@25
  api today()@29
  api read_json(p, default=None)@33
  api write_json(p, obj)@40
  api append_jsonl(p, obj)@48
  api read_jsonl(p)@55
  api emit(obj, code=0)@70 — Machine-first output: exactly one JSON object on stdout.
  api class DhError@77
  api which(cmd)@83 — Resolve a launcher to a real executable (Windows PATH carries npm.cmd, not npm).
  api rmtree(path)@88 — Delete a tree even where files are read-only (git objects on Windows); a no-op when missing.
  api run(argv, cwd=None, timeout=900, env=None, check=False)@106
  api project_root(p=None)@123 — The project: --project, else the nearest parent with .deckhand/ or package.json, else cwd.
  api slugify(s, n=48)@137
  api package_json(root)@142
  api package_manager(root)@146
  api pm_run(root, script)@154
  api git_files(root)@159 — Tracked + untracked-not-ignored files (falls back to a walk without node_modules/.git).
  api is_text(p)@177
  const: SKILL@13 DATA@14 REFS@15 TEMPLATES@16 TRYON@17 TEXT_EXT@173
#### dhlib/verify.py  (172 lines, py)
  | REVIEW: one command, evidence rows, a red row blocks the deploy gate.
  | Rows: typecheck · lint (advisory) · build · prod-clean (no try-on stamp in the build) · tryon-closed ·
  | secrets · leaks/honesty · routes (every planned static route + every internal link on the home page
  | answers < 400 — on the production build, served on a free port for the check) · a11y basics (advisory) · dependency audit (advisory).
  | Writes .deckhand/verify.json + .deckhand/VERIFY.md.
  deps: dhlib/brand.py, dhlib/build.py, dhlib/swap.py, dhlib/util.py
  used-by: dhlib/cli.py, dhlib/harvest.py
  api run_verify(root, url=None, skip=…, allow=…)@43
  int: _Links@22 _get@34
  const: SECRET_RX@19
#### dhlib/vet.py  (113 lines, py)
  | VET: nothing enters the pool on trust — every base is measured, then judged against written criteria.
  | A found repo (by the owner, by the agent, by anyone) goes through `dh pool vet owner/repo` (report only) or
  | `dh pool add owner/repo` (report, then added only if accepted). The verdict is a pure function of the
  | measured evidence: same repo state -> same verdict, with every reason written out, and — when refused —
  | what WOULD be accepted, so the owner knows what to look for instead.
  | Hard criteria (any fail = refused):
  |   licence      a permissive licence (data/licenses.json: MIT, Apache-2.0, BSD-2/3, ISC, 0BSD, Unlicense)
  |   consistent   package.json "license" does not contradict it (UNLICENSED, "SEE LICENSE IN", a copyleft id)
  |   alive        not archived; pushed within the last 3 years
  |   runnable     a web app: package.json with a build script and a dev or start script, a known framework
  |   clean        no committed secrets (.env, keys, service-account files)
  |   free         no paid/private packages (Tailwind Plus, MUI X Pro, AG Grid Enterprise, Font Awesome Pro …)
  | Soft criteria (warnings, and ranking penalties in `dh pool query`):
  |   paid tier mentioned in the README · stale (>1 year) · few stars · huge monorepo · rented services to swap ·
  |   an old Node engine · a framework try-on cannot use (Astro/Svelte/Nuxt)
  deps: dhlib/util.py
  used-by: dhlib/pool.py
  api acceptable_text()@34
  api verdict(row, mine=False)@46 — row = pool.measure() output (with its `evidence`). Pure: no network, no clock besides `now` for age.
  api evidence(pkg, tree, readme, has_package)@100 — What verdict() needs, extracted from the files pool.measure() already downloads.
  int: _days@39
  const: POLICY@27 ACCEPTED@28 REFUSED@29 TRYON_FRAMEWORKS@30 WEB_FRAMEWORKS@31
#### tryon/catalog-build.mjs  (149 lines, js)
  | catalog-build.mjs — (re)build data/components.index.json from the licensed registries.
  | Maintainer tool; owners never need it (the index ships with the skill).
  |   node tryon/catalog-build.mjs [--snapshots <dir>] [--out data/components.index.json]
  | Sources (data/registries.json): tailark-oss is read from its MIT GitHub source (the registry
  | definitions are TypeScript, parsed with the vendored AST parser — no regex over code); every
  | shadcn-schema registry is read from its live registry.json, or from --snapshots/<id>@<style>.json.
  | Only registries whose licence is MIT/Apache-2.0 are indexed; `refused` entries never are.
  deps: tryon/lib/ast.cjs, tryon/lib/regmap.mjs, tryon/lib/slots.mjs
  int: opt@28 get@32 lit@40 tailark@67 readSnapshot@106 shadcnSchema@117 main@130
  const: HERE@23 SKILL@24 OK_LICENSES@25 SNAP@29 OUT@30 NUM@65
#### tryon/cli.mjs  (157 lines, js)
  | tryon — Deckhand's live component swap. One JSON object per command on stdout; exit 0 = ok.
  |   node tryon/cli.mjs setup    [--project .]                      wire the dev-only stamp (restart dev)
  |   node tryon/cli.mjs serve    [--project .] [--port 3999] [--target http://127.0.0.1:3000]
  |   node tryon/cli.mjs doctor   [--project .]                      is everything wired? (no writes)
  |   node tryon/cli.mjs slots    [--project .]                      what can be tried here
  |   node tryon/cli.mjs query    --slot hero [--project .]          ranked candidates (no writes)
  |   node tryon/cli.mjs inspect  --file f --line n --col c [--slot s]
  |   node tryon/cli.mjs try      --file f --line n --col c --slot s [--count 4] [--no-install] [--registry r]
  |   node tryon/cli.mjs show     --id S --idx N                     make variant N the visible one
  |   node tryon/cli.mjs keep     --id S [--idx N]                   collapse to N, bake the text, graduate
  |   node tryon/cli.mjs discard  --id S                             byte-exact restore
  |   node tryon/cli.mjs save     --id S [--name n]                  kept component -> personal library
  |   node tryon/cli.mjs status   [--project .]
  |   node tryon/cli.mjs clean    [--project .]                      discard open sessions, unwire
  | AI draft (no licensed design fits, or the owner asks) — the agent writes ONE variant, scripts gate it:
  |   node tryon/cli.mjs draft      --file f --line n --col c --slot s [--note "…"]  |  --session S
  |   node tryon/cli.mjs drafts     [--wait [--timeout 1800]]          pending requests (+ each brief path)
  |   node tryon/cli.mjs draft-check --id D                            run the gates, write nothing
  |   node tryon/cli.mjs draft-done  --id D                            gate + show it (labelled AI-generated)
  | A registry someone found — vetted (licence, paywall, schema, usable items) before it is indexed:
  |   node tryon/cli.mjs registry vet|add --index https://…/registry.json --repo owner/name [--id x]
  |   node tryon/cli.mjs registry list | registry remove --id x
  deps: tryon/lib/catalog.mjs, tryon/lib/draft.mjs, tryon/lib/engine.mjs, tryon/lib/library.mjs, tryon/lib/project.mjs, tryon/lib/setup.mjs, tryon/lib/vet.mjs, tryon/server.mjs
  int: out@52 need@53 main@55
#### tryon/compose.mjs  (162 lines, js)
  | compose.mjs — a page from licensed blocks + the plan's copy, no code generation.
  |   node tryon/compose.mjs --project . --page app/page.tsx --sections hero,features,pricing,faq,cta,footer
  |                          [--copy .deckhand/copy.json] [--registry tailark-oss]
  | copy.json (the one input a model writes, from the plan — words, never code):
  |   { "hero": { "heading": "…", "text": ["…"], "actions": [{ "label": "…", "href": "/order" }], "image": { "src": "/x.jpg", "alt": "…" } },
  |     "pricing": { "heading": "…", "items": [{ "title": "…", "price": "€9", "text": "…", "bullets": ["…"], "action": { "label": "…", "href": "…" } }] },
  |     "footer": { "text": ["…"], "columns": [{ "title": "…", "links": [{ "label": "…", "href": "/x" }] }], "social": ["https://instagram.com/…"] } }
  | Footer/navbar menus default to the plan (.deckhand/sitemap.json nav + page titles); social rows keep only
  | the networks in copy or brief.brand.social. A design's own form is hidden unless the section says
  | "form": true (the plan then wires it) — no newsletter box that posts nowhere.
  | Every section lands in components/sections/<slug>/, the page imports them in order; each is then
  | swappable with try-on. Copy the design has no room for is reported, never silently dropped.
  deps: tryon/lib/catalog.mjs, tryon/lib/engine.mjs, tryon/lib/materialize.mjs, tryon/lib/project.mjs, tryon/lib/sitelinks.mjs, tryon/lib/transplant.mjs
  int: opt@31 out@37 esc@39 unit@40 origFromCopy@43 main@65
#### tryon/lib/ast.cjs  (133 lines, js)
  | ast.cjs — source-exact parsing for try-on.
  | Contract: every node carries `start`/`end` (UTF-16 offsets into the exact text that was parsed),
  | so every edit is a string splice on the original bytes: no reprint, no formatting drift.
  | Parser = vendored @babel/parser (zero dependencies). The project's own TypeScript is NOT used:
  | typescript@7 ships no JS compiler API, which silently broke the v1 engine on fresh projects.
  deps: tryon/vendor/babel-parser.cjs
  used-by: tryon/catalog-build.mjs, tryon/lib/draft.mjs, tryon/lib/engine.mjs, tryon/lib/materialize.mjs, tryon/lib/registry.mjs, tryon/lib/setup.mjs, tryon/lib/sitelinks.mjs, tryon/lib/sitetheme.mjs, tryon/lib/stamp.cjs, tryon/lib/theme.mjs, tryon/lib/transplant.mjs, tryon/lib/tune.mjs, tryon/test/draft.test.mjs, tryon/test/engine.test.mjs, tryon/test/sections.test.mjs, tryon/test/setup-theme-server.test.mjs, tryon/test/stamp.test.mjs, tryon/test/transplant.test.mjs, tryon/test/tune.test.mjs
  api parse(file, text)@22 — Parse or throw a typed error (code PARSE_FAILED) carrying the first parser message.
  api walk(node, enter, parent = null, key = null)@45 — Depth-first walk; `enter(node, parent, key)` returning false skips the subtree.
  api jsxName(n)@61 — `<motion.div>` -> "motion.div", `<svg:rect>` -> "svg:rect", `<Foo>` -> "Foo".
  api isHostName(name)@69
  api offsetOf(text, line, col)@72 — 1-based line/col -> offset.
  api findElementAt(ast, text, line, col)@83 — The JSXElement whose opening `<` sits exactly at (line, col), 1-based.
  api elementContaining(ast, offset)@96 — Innermost JSXElement containing `offset`.
  api attr(el, name)@106
  api attrLiteral(a)@115 — Literal value of an attribute (string or {"string"} / {`tpl`} without expressions), else null.
  api lineCol(text, offset)@127
  int: pluginsFor@15
  const: BASE_PLUGINS@12 RETRY_PLUGINS@13
#### tryon/lib/catalog.mjs  (84 lines, js)
  | catalog.mjs — deterministic candidate ranking. Same inputs -> same order, always.
  | Personal library items (r = "mine") rank first; base mismatches are hidden AND counted.
  deps: tryon/lib/project.mjs, tryon/lib/slots.mjs
  used-by: tryon/cli.mjs, tryon/compose.mjs, tryon/lib/draft.mjs, tryon/lib/engine.mjs, tryon/lib/library.mjs, tryon/lib/materialize.mjs, tryon/lib/vet.mjs, tryon/server.mjs, tryon/test/vet.test.mjs
  api SKILL_DIR@13
  api libraryDir()@15
  api loadCatalog()@19
  api rank(items, { slot, prof, exclude = [], registry = null })@43
  api kitOf(x)@75 — A design family: one Tailark kit (dusk / mist / veil) or one registry.
  api slotsSummary(items, prof)@77
  int: baseScore@36
  const: HERE@12
#### tryon/lib/draft.mjs  (282 lines, js)
  | draft.mjs — the AI fallback for try-on.
  | When no licensed design fits an element (or the owner asks for one), the project's own AI agent
  | writes ONE variant from a brief. Scripts then prove it before the owner sees it:
  |   - every word, link, image and bullet of the owner's element is on it (nothing dropped);
  |   - colours are the site's tokens (no hex / rgb / arbitrary colour values);
  |   - imports are the project's own primitives or installed packages (nothing to install);
  |   - images are the owner's or the neutral placeholder; links go only where the owner's went;
  |   - no data fetching, no env access.
  | Words the owner never wrote are listed as AI-written, never presented as the owner's facts. The
  | variant is labelled AI-generated in the bar, the session, the file header and the keep result.
  | The agent is called once per request — never per click (the v1 polling loop stays dead).
  deps: tryon/lib/ast.cjs, tryon/lib/catalog.mjs, tryon/lib/engine.mjs, tryon/lib/project.mjs, tryon/lib/registry.mjs, tryon/lib/slots.mjs, tryon/lib/transplant.mjs
  used-by: tryon/cli.mjs, tryon/server.mjs, tryon/test/draft.test.mjs
  api filesDir(root, id)@33
  api loadDraft(root, id)@41
  api listDrafts(rootIn, { state } = {})@50
  api publicDraft(d)@58 — What the overlay/CLI shows about a request (the brief itself is for the agent).
  api requestDraft(rootIn, { file, line, col, slot, note, session } = {})@67 — The owner (overlay) or the agent (CLI) asks for an AI variant of one element.
  api checkDraft(rootIn, id)@163 — Deterministic checks of what the agent wrote.
  api completeDraft(rootIn, id, { install = false, onProgress } = {})@241 — The agent is done writing: gate it, then show it.
  int: now@30 draftsDir@31 docPath@32 rel@34 saveDraft@46 brief@103 specToFile@155 dynamicOf@222 origFromBrief@226
  const: CLI@35 TOKEN_CLASSES@36 TOOLING@39
#### tryon/lib/engine.mjs  (913 lines, js)
  | engine.mjs — one try-on session, end to end, with no LLM in the loop.
  |   open()    click target (file:line:col) -> N ranked, fetched, themed, content-transplanted
  |             variants written ONCE into the source inside a wrapper; HMR renders them; cycling
  |             is client-side (display toggles) so browsing variants costs zero writes.
  |   show()    persist which variant is visible (headless agents / reloads).
  |   keep()    the wrapper collapses to the chosen variant; literal copy is baked into the
  |             component; its folder graduates to components/sections|ui-kit; losers are deleted.
  |   discard() byte-exact restore (or a surgical unwrap if the file changed since).
  | State: <project>/.deckhand/tryon/{sessions/*.json,backups/} — the journal is the truth.
  deps: tryon/lib/ast.cjs, tryon/lib/catalog.mjs, tryon/lib/materialize.mjs, tryon/lib/project.mjs, tryon/lib/sitelinks.mjs, tryon/lib/slots.mjs, tryon/lib/theme.mjs, tryon/lib/transplant.mjs
  used-by: tryon/cli.mjs, tryon/compose.mjs, tryon/lib/draft.mjs, tryon/lib/library.mjs, tryon/server.mjs, tryon/test/draft.test.mjs, tryon/test/engine.test.mjs, tryon/test/sections.test.mjs
  api class TryonError@29
  api PLACEHOLDER@34
  api ensurePlaceholder(prof)@37 — A neutral image placeholder in public/ (a registry's demo screenshot is never shown as the owner's).
  api stateDir(root)@51
  api loadSession(root, id)@53
  api listSessions(root)@62
  api install(prof, pkgs, log)@143
  api brandName(root)@152 — The owner's brand name: brief first, then package.json.
  api ensureTokens(prof, probe = {})@158 — Ensure the semantic token layer exists (idempotent; recorded for `clean`).
  api open(rootIn, opts)@195 — Open a session.
  api publicSession(s)@365
  api logoLocalsFor(stage, entryCode, slot)@376 — Local names the entry imports from brand-logo files — hidden unless the slot IS a logo cloud.
  api show(rootIn, id, idx)@388 — Make variant `idx` the visible one in source (headless / persistence).
  api pruneFolder(root, dirRel, entryRel)@449 — Drop import specifiers nobody references any more; delete folder files nothing imports.
  api bake(root, fileRel, local, variant)@497 — Bake the kept variant: literal copy goes INTO the component, show/hide switches are resolved (hidden demo buttons and l…
  api flatText(s)@700 — Text reduced to letters and digits (markup, quotes, punctuation and spacing never decide a match).
  api literalFit(root, dirRel, orig, dynamic = [])@706 — How much of the owner's content a hand-written (AI) component really holds: every text unit, link target, image and bul…
  api demoTexts(root, dirRel, ownerTexts = [])@731
  api recordDemoCopy(root, entries)@763
  api keep(rootIn, id, idx)@790
  api discard(rootIn, id, { reason } = {})@857
  api inspect(rootIn, { file, line, col, slot })@894 — Where is this element, what could it be swapped with? (no writes)
  int: sha@48 now@49 sessionsDir@52 saveSession@58 newId@68 identifiers@73 uniqueName@74 importInsertPos@81 indentAt@91 findWrapper@97 variantChildren@109 innerSrc@112 primitiveUsage@119 appendCss@169 removeCss@178 removeMarkedImports@413 referencedByOtherOpen@425 markupOnly@433 recordNotice@777
  const: PLACEHOLDER_SVG@33 SAFE_GLOBALS@429 ENT@698 NOTICE_FILE@775
#### tryon/lib/library.mjs  (74 lines, js)
  | library.mjs — the owner's personal component pool ("mine"), shared by try-on and `dh`.
  |   <library>/components/<name>/...        the files, exactly as kept (the owner's version)
  |   <library>/components.index.json        catalog rows (r = "mine"), ranked first in every query
  | Saving refuses what cannot be honoured later: no recorded licence, or a credential-shaped string.
  deps: tryon/lib/catalog.mjs, tryon/lib/engine.mjs, tryon/lib/project.mjs, tryon/lib/slots.mjs
  used-by: tryon/cli.mjs, tryon/server.mjs, tryon/test/engine.test.mjs
  api readIndex()@19
  api saveDir({ dir, entry, name, slot, base = 'any', lic, source, title, deps = [] })@29 — Save a directory of component files as a library item.
  api saveToLibrary(rootIn, sessionId, { name } = {})@50
  api listLibrary()@62
  api removeFromLibrary(name)@64
  int: indexPath@18 writeIndex@22
  const: SECRET@16
#### tryon/lib/materialize.mjs  (326 lines, js)
  | materialize.mjs — turn a catalog item into files inside the project, self-contained:
  |   <componentsDir>/dh-tryon/<slug>/<slug>.tsx     the entry component
  |   <componentsDir>/dh-tryon/<slug>/<helper>.tsx   every file it needs that the project lacks
  | Rules (the reason v1 swaps broke at import time):
  |  - EVERY file the item needs is fetched (v1 staged only the entry file);
  |  - a ui primitive the project already has (button, card, input…) is NOT fetched: the import is
  |    pointed at the project's own component, so the variant renders in the site's design system;
  |  - `lib/utils` resolves to the project's cn(); a project without one gets a local copy;
  |  - bare imports become npm deps (reported; installed by the caller, batched);
  |  - palette classes -> semantic tokens; 'use client' added when hooks/handlers need it;
  |  - an attribution header (source URL + licence) is written into every file.
  deps: tryon/lib/ast.cjs, tryon/lib/catalog.mjs, tryon/lib/project.mjs, tryon/lib/registry.mjs, tryon/lib/theme.mjs
  used-by: tryon/compose.mjs, tryon/lib/engine.mjs
  api cn(...inputs: ClassValue[])@30
  api slugOf(item)@36
  api pascal(s)@37
  api compatFixes(file, code)@50 — Upstream typing bugs fixed deterministically so a kept file passes `next build` type checks.
  api entryExport(file, code)@125 — The export a variant usage binds to: default, else the first PascalCase named export.
  api fetchBundle(prof, item)@133 — Fetch (cached) the normalized bundle for a catalog item.
  api writeBundle(prof, item, bundle, { baseDir } = {})@234 — Write a bundle into <componentsDir>/dh-tryon/<slug>/.
  int: projectProvides@40 needsClient@99 exportsOf@106 jsonBundle@183
  const: UTILS_SRC@27 BUILTIN@34
#### tryon/lib/project.mjs  (117 lines, js)
  | project.mjs — a measured profile of the target project (never assumed):
  | framework, aliases, where ui/lib live, package manager, installed deps, Tailwind major,
  | the global stylesheet, whether shadcn-style semantic tokens exist, and the primitive base.
  used-by: tryon/cli.mjs, tryon/compose.mjs, tryon/lib/catalog.mjs, tryon/lib/draft.mjs, tryon/lib/engine.mjs, tryon/lib/library.mjs, tryon/lib/materialize.mjs, tryon/lib/setup.mjs, tryon/lib/sitetheme.mjs, tryon/lib/tune.mjs, tryon/server.mjs, tryon/test/tune.test.mjs
  api stripJsonComments(s)@13 — tsconfig/jsconfig allow comments + trailing commas.
  api detectProject(rootIn)@33
  api specFor(prof, relFile)@107 — Import specifier for a project-relative file path (posix, without extension).
  api depInstalled(prof, name)@115 — Is an npm package resolvable from the project (declared AND present in node_modules)?
  int: exists@9 readJson@10
  const: PM@31
#### tryon/lib/registry.mjs  (147 lines, js)
  | registry.mjs — fetch a catalog item into a normalized, self-contained bundle:
  |   { files: [{ key, path, content }], entryKey, deps[], rdeps[], css, cssVars, origin, sourceUrl }
  | Two transports, tried in order, both cached on disk (a click never refetches):
  |   json — a shadcn-schema registry item (`files[].content` required; content-less = refused)
  |   gh   — MIT GitHub source: fetch the entry file, then follow every `@/registry/...` and
  |          relative import recursively (bounded), collecting bare imports as npm deps.
  | Guard rails: https only (http only on loopback, for fixtures), size cap, file-count cap,
  | timeout. Offline replay: DH_FIXTURES=<dir> serves recorded responses; DH_RECORD=<dir> records.
  deps: tryon/lib/ast.cjs
  used-by: tryon/lib/draft.mjs, tryon/lib/materialize.mjs, tryon/lib/vet.mjs, tryon/test/vet.test.mjs
  api cacheDir()@25
  api keyOf(url)@29
  api getText(url, { cache = true } = {})@31
  api importsOf(file, text)@71 — Module specifiers of a source file (static imports, re-exports, literal dynamic imports).
  api pkgName(spec)@88 — "motion/react" -> "motion"; "@radix-ui/react-slot/x" -> "@radix-ui/react-slot".
  api followGh({ repo, entryPath, importRoots = { '@/registry/': 'registry/' }, stop = () => null })@100 — Follow a GitHub source graph.
  api fetchJsonItem(url)@142 — Fetch a shadcn-schema JSON item.
  int: isBare@93 isNodeBuiltin@94
  const: MAX_BYTES@21 MAX_FILES@22 BUILTIN@23
#### tryon/lib/regmap.mjs  (37 lines, js)
  | regmap.mjs — one mapping from a shadcn-schema registry index to catalog items, used by the maintainer
  | build (catalog-build.mjs) and by an owner's vetted registries (`tryon registry add`): same rules, same ids.
  deps: tryon/lib/slots.mjs
  used-by: tryon/catalog-build.mjs, tryon/lib/vet.mjs
  api mapShadcnItems(reg, idx, style = null, base = 'any')@8 — reg: { id, item (url template with {name}/{style}), license, mirror?, gh?, free_types? }
  api baseOf(items)@32 — The primitive base a registry's items are written for, from their dependencies.
#### tryon/lib/setup.mjs  (199 lines, js)
  | setup.mjs — put the dev-only stamp into a project, and take it out again byte-exact.
  | Next.js: next.config.* gets ONE wrapped export — `export default withDeckhandTryon(config)` —
  |   the plugin (copied into .deckhand/tryon/runtime/) adds the Turbopack rule (and the webpack
  |   rule under --webpack) only when NODE_ENV=development. The user's config object is untouched.
  | Vite: `dhTryon()` is prepended to `plugins: [...]` (apply: 'serve' — never in a build).
  | Everything is journaled in .deckhand/tryon/setup.json; `.deckhand/tryon/` is gitignored.
  deps: tryon/lib/ast.cjs, tryon/lib/project.mjs, tryon/lib/theme.mjs
  used-by: tryon/cli.mjs, tryon/test/setup-theme-server.test.mjs
  api withDeckhandTryon(config)@61
  api readJournal(root)@69
  api patchNextConfig(file, code)@87 — Wrap the default export of next.config.* (ESM/TS) or module.exports (CJS).
  api patchViteConfig(file, code)@110
  api setup(rootIn)@130
  api unsetup(rootIn, { keepTokens = null } = {})@169 — Undo setup.
  int: sha@23 nextVersion@30 apply@34 journalPath@68 copyRuntime@73 findConfig@84
  const: HERE@20 TRYON@21 MARK@22 NEXT_PLUGIN@25 LOADER@28
#### tryon/lib/sitelinks.mjs  (179 lines, js)
  | sitelinks.mjs — a footer or navbar shows the site's own routes, never the design's demo menu.
  | Source of truth, in order: copy.json (`footer.columns`, `footer.social`, `navbar.links`), then the
  | plan (`.deckhand/sitemap.json` nav.header / nav.footer, page titles as labels), then the brief's
  | `brand.social`. The design's literal link arrays are rewritten in place (AST-exact): flat menus get
  | the owner's routes, "GitHub / Discord / X" rows keep only the networks the owner really has (with the
  | design's icons), column menus get the owner's columns. Nothing is invented: no source → no change.
  deps: tryon/lib/ast.cjs
  used-by: tryon/compose.mjs, tryon/lib/engine.mjs, tryon/test/sections.test.mjs
  api siteLinks(root, copy = {})@35 — The owner's links: { header[], footerCols[{title, links[]}], social[] } — or null when nothing is known.
  api fillLinks(file, code, links, slot, { ts = /\.tsx?$/.test(file) } = {})@118 — Fill the design's literal link arrays in `code` for a footer/navbar.
  api linkTexts(links)@176 — Every word the owner's links put on the page (so the demo-copy ledger never flags them).
  api FORM_SLOTS@179 — Slots whose form IS the purpose (the plan wires it); every other design form is hidden by default.
  int: networkOfUrl@29 networkOfLabel@30 readJson@32 keyName@74 strVal@75 unwrap@76 linkShape@78 linkArray@89 columnShape@95 relink@108
  const: LABEL_KEYS@14 HREF_KEYS@15 SOCIAL@16 NETWORK@17
#### tryon/lib/sitetheme.mjs  (267 lines, js)
  | sitetheme.mjs — the whole site's look in six knobs, previewed instantly, applied exactly.
  |   accent       a curated palette (or any hex) → --primary / --primary-foreground / --ring, light + dark
  |   neutrals     as is · neutral · warm · cool → background, card, muted, border… tinted consistently
  |   corners      sharp · soft · as is · round · pill → --radius
  |   density      compact · snug · as is · airy · spacious → --spacing (Tailwind 4 spacing scale)
  |   headlines    smaller · as is · larger · display → --text-2xl … --text-9xl
  |   fonts        body + heading from a curated Google Fonts list (Next.js next/font: an exact AST rewrite)
  | Colours, radius, spacing and type are CSS variables: the overlay previews them on :root with the exact
  | values that get written, in one marked block at the end of globals.css (`dh:theme`). Undo restores the
  | previous files byte-exact.
  deps: tryon/lib/ast.cjs, tryon/lib/project.mjs
  used-by: tryon/server.mjs, tryon/test/tune.test.mjs
  api ACCENTS@22
  api FONTS@27
  api KNOBS@36
  api themeVars(v = {})@74 — The exact variable values for a choice of knobs — the overlay previews these, apply writes these.
  api themeCss(v, fonts = {})@96
  api readFonts(prof)@126 — The layout's next/font/google setup: { file, fonts: [{ name, callee, variable, start, end }] }
  api rewriteFonts(prof, { body, heading } = {})@155 — Rewrite the body / heading next/font families (AST-exact).
  api themeState(rootIn)@224
  api themeApply(rootIn, v = {})@236
  api themeUndo(rootIn)@259
  int: oklchOf@47 fmt@57 neutrals@59 stripBlock@108 layoutFile@117 roleOf@152 stateDir@222
  const: TEXT@43 BEGIN@44 END@45
#### tryon/lib/slots.mjs  (66 lines, js)
  | slots.mjs — the one slot vocabulary shared by the catalog, the overlay and the planner.
  | A slot is WHAT a thing is for (hero, pricing, button), never what it is called upstream.
  used-by: tryon/catalog-build.mjs, tryon/lib/catalog.mjs, tryon/lib/draft.mjs, tryon/lib/engine.mjs, tryon/lib/library.mjs, tryon/lib/regmap.mjs, tryon/server.mjs
  api BLOCK_SLOTS@5 — slots.mjs — the one slot vocabulary shared by the catalog, the overlay and the planner.
  api UI_SLOTS@9
  api EFFECT_SLOTS@14
  api TAILARK_CATEGORY@16
  api slotFromName(name)@46
  api kindOf(slot)@52
  api COMPATIBLE@60 — Slots a click on one slot may be swapped with (like-for-like, plus close cousins).
  const: NAME_RULES@24
#### tryon/lib/stamp.cjs  (68 lines, js)
  | stamp.cjs — the dev-only source map from DOM to JSX.
  | Every JSX opening element gets `data-dh="<rel/path.tsx>:<line>:<col>"` inserted right after its
  | tag name (before any spread), so:
  |   - a host element (`<section>`) carries its own source location into the DOM;
  |   - a component usage (`<Button>`) passes its usage location as a prop; components that spread
  |     props onto their root (every shadcn primitive) let the USAGE site win in the DOM, which is the
  |     site the owner clicked.
  | AST-exact (vendored babel): no regex over source, so generics (`useState<T>()`), strings, comments
  | and multi-line tags are never touched. A file that fails to parse is returned unchanged — the
  | bundler reports the real error itself.
  deps: tryon/lib/ast.cjs
  used-by: tryon/loader.cjs, tryon/test/stamp.test.mjs, tryon/vite.mjs
  api stamp(code, rel)@33 — Stamp a source file.
  api parseStamp(value)@62 — Parse a stamp value "rel/path.tsx:12:5" (the path may itself contain ':' on Windows drives).
  int: shouldSkip@24
  const: SKIP@18 ATTR@22
#### tryon/lib/theme.mjs  (183 lines, js)
  | theme.mjs — make a registry component wear the SITE's colours, deterministically.
  | 1. normalizeClasses(): hard-coded Tailwind palette classes become semantic tokens
  |    (bg-zinc-100 -> bg-muted, bg-indigo-600 -> bg-primary, text-gray-500 -> text-muted-foreground);
  |    `dark:` palette twins are dropped because tokens already flip with the theme.
  |    Only class-list strings are touched (className, cn/clsx/cva/tv args) — AST-located.
  | 2. tokenLayer(): for a project WITHOUT semantic tokens, the missing ones are derived from the
  |    site's own --background/--foreground (color-mix), so blocks inherit its palette and dark mode.
  |    An accent probed from the live page (overlay) becomes --primary.
  deps: tryon/lib/ast.cjs
  used-by: tryon/lib/engine.mjs, tryon/lib/materialize.mjs, tryon/lib/setup.mjs, tryon/test/setup-theme-server.test.mjs
  api normalizeClassList(str)@54
  api normalizeClasses(file, code)@68 — Rewrite class-list string literals in a source file.
  api TOKENS_BEGIN@116
  api TOKENS_END@117
  api tokenLayer(cssText, probe = {})@123 — The token layer for a Tailwind v4 stylesheet.
  api stripTokenLayer(cssText)@150
  api itemCss(item, tag)@161 — A registry item's cssVars/css as a Tailwind v4 block (keyframes + theme animation vars).
  int: mapOne@22
  const: NEUTRAL@17 ACCENT@18 SHADE@19 CLASS_FNS@20 TOKENS@101
#### tryon/lib/transplant.mjs  (952 lines, js)
  | transplant.mjs — "their design, your words", deterministically (no model call).
  | extractUnits(): the clicked JSX subtree -> content units in document order. A unit is the
  |   nearest content-bearing element (h1-h6, p, a/Link/Button, li, label, blockquote …); its source is
  |   the deepest element holding all the text (so icons beside a label survive), taken VERBATIM:
  |   inner markup and in-scope expressions like {t('title')} are carried as-is.
  |   Lists rendered with `arr.map(…)` over a literal array in the same file are UNROLLED: each
  |   item's fields are evaluated, so a pricing grid carries every plan, price and feature.
  | parameterize(): the candidate component -> slots. Static units become
  |   `{content.heading1 ?? <>demo</>}`; a `.map()` over the candidate's own demo array becomes a
  |   list slot `(content.list1 ? content.list1.map(merge-with-demo) : demo).map(…)`.
  | bind(): pair units by role and order; pair lists item by item, field by role.
  | The usage replaces the clicked element in place, so every carried expression stays in scope.
  deps: tryon/lib/ast.cjs
  used-by: tryon/compose.mjs, tryon/lib/draft.mjs, tryon/lib/engine.mjs, tryon/test/sections.test.mjs, tryon/test/transplant.test.mjs
  api roleOf(name)@24
  api litValue(node)@134 — JS value of a literal AST node; undefined when anything is non-literal.
  api extractUnits(code, el, ast = null)@522 — Content of the clicked element.
  api parameterize(file, code, exp, opts = {})@600 — Parameterize the candidate entry file -> { code, slots[], lists[], prop, removed[] }.
  api bind(orig, cand, opts = {})@800 — Pair original content with candidate slots -> { props: [[key, src]], carried[], dropped[], demo[] }.
  api contentProp(prop, props)@939
  api contentCount(orig)@945 — How much content the original holds (the fit denominator).
  api shapeOf(u)@949
  int: isBlockEl@36 withPrice@41 isStaticExpr@46 hasStaticText@48 isDynamicChild@52 childExpressions@62 directContent@65 inlineContent@66 textHost@72 containsBlock@78 actionKind@88 textRange@97 findHref@108 textOf@115 topArrays@159 mapCall@171 binding@178 fieldOf@196 callbackJsx@203 escapeJsxText@217 resolvedSrc@220 resolvedText@247 collect@257 listFields@427 siblingGroups@468 cardGroups@499 attrValueSrc@514 findComponentFn@552 returnedJsx@576
  const: BLOCK_TAGS@21 COMPOUND@35 CONTENT_ROLES@37 ITEM_CLASSES@38 PRICE@39 COPYRIGHT@40 CHROME@593
#### tryon/lib/tune.mjs  (225 lines, js)
  | tune.mjs — adjust a picked element's design with knobs, not prompts (zero model calls).
  | Every knob is a deterministic Tailwind class transform applied to the element's subtree in its own file:
  |   density  -2..+2   spacing (p/m/gap/space-*) × 0.6 · 0.8 · 1 · 1.25 · 1.5
  |   size     -2..+2   text sizes: headline sizes move a full step per notch, body sizes half as much
  |   weight   -2..+2   font weights (existing ones), clamped light..black
  |   corners  sharp · soft · · round · pill   rounded-* (rounded-full circles are left alone except by sharp)
  |   depth    flat · subtle · · raised        shadow-* ; raised gives bordered cards a lift
  |   contrast soft · · crisp                  muted text ↔ foreground, faint borders ↔ full borders
  |   width    -2..+2   max-w-* steps
  | Presets (the Impeccable verbs, as knob combinations): quieter, bolder, airy, compact, clarity, softer, sharper.
  | A tune session always recomputes from the ORIGINAL source (never cumulative drift) and writes it; the dev
  | server's HMR shows it — so what you see is exactly what you keep. Reset restores the file byte-exact.
  deps: tryon/lib/ast.cjs, tryon/lib/project.mjs
  used-by: tryon/server.mjs, tryon/test/tune.test.mjs
  api PRESETS@25
  api DIALS@34
  api tuneToken(tok, d, ctx = {})@54 — One class token (with its variant prefixes, e.g.
  api tuneClassList(list, d, ctx = {})@112 — A whole class string; `raised` adds a lift to bordered, rounded cards that had no shadow.
  api tuneElement(code, el, d)@125 — Transform every class string inside `el` (className literals, template quasis, cn()/clsx() strings).
  api dialsOf(dials = {}, preset = null)@159
  api tuneOpen(rootIn, { file, line, col })@172
  api tuneSet(rootIn, id, { dials = {}, preset = null } = {})@188
  api tuneKeep(rootIn, id)@205
  api tuneReset(rootIn, id)@214
  int: clamp@50 num@51 isControl@121 isHeading@122 dir@163 sha@164 load@165 save@170
  const: DENSITY@44 SIZES@45 WEIGHTS@46 RADII@47 SHADOWS@48 WIDTHS@49
#### tryon/lib/vet.mjs  (117 lines, js)
  | vet.mjs — a component registry enters try-on only after it is measured against written criteria.
  |   tryon registry vet --index https://x.dev/r/registry.json --repo owner/name [--id x]   report only
  |   tryon registry add  …same…                                                           report, then add
  | Hard criteria (any fail = refused, with the reason and what would be accepted):
  |   not refused   not on the refused list (data/registries.json: AGPL, Commons Clause, paid tiers, bot walls)
  |   licence       the source repository's licence is permissive (data/licenses.json — the same policy as bases)
  |   schema        the index is a shadcn-schema registry with ui/block/component items
  |   usable        at least one item maps to a try-on slot (hero, pricing, button…)
  |   free          sampled items download without an account and carry their source (no 401/402/403,
  |                 no content-less "pro" items) — a paywall is refused, whatever the licence says
  | Accepted -> ~/.deckhand/registries.json (config) + ~/.deckhand/catalog/registry-<id>.json (its items),
  | which the catalog loads beside the shipped one. Same inputs -> same verdict.
  deps: tryon/lib/catalog.mjs, tryon/lib/registry.mjs, tryon/lib/regmap.mjs
  used-by: tryon/cli.mjs, tryon/test/vet.test.mjs
  api acceptable()@27
  api vetRegistry({ index, repo, id, item: itemTemplate, sample = 3 })@31
  api addRegistry(opts)@89
  api listRegistries()@107
  api removeRegistry(id)@111
  int: deckhandHome@25 slug@29
  const: POLICY@23 REGISTRIES@24
#### tryon/loader.cjs  (24 lines, js)
  | Deckhand try-on stamp loader — webpack AND Turbopack (Next.js `turbopack.rules`).
  | Dev-only twice over: `setup` registers it only under NODE_ENV=development, and it is inert here
  | outside development (a Turbopack rule `condition` is a matcher, not an env gate).
  deps: tryon/lib/stamp.cjs
  used-by: tryon/test/setup-theme-server.test.mjs
  api deckhandStampLoader(source)@10
  int: done@12
#### tryon/overlay.js  (714 lines, js)
  | Deckhand try-on overlay — injected by the local helper into the owner's dev site (never prod).
  | Pick any element -> choose what it is -> N real, licensed variants appear IN the page, wearing the
  | site's colours and the owner's own words -> ←/→ to compare (instant, client-side) -> Keep / Discard.
  | No licensed design fits (or the owner wants another)? "Ask AI to draft one": the agent writes it once,
  | the engine gates it, and it appears here labelled AI-generated.
  | The page is never edited here: the helper writes source, the dev server's HMR re-renders.
  int: api@17 el@68 fiberOf@81 firstHost@85 crumbsFor@89 q@114 guessSlot@115 defaultCrumb@146 probe@154 place@172 ours@173 stampOf@174 onMove@175 onClick@188 startPicking@196 stopPicking@197 closePanel@208 openPanel@209 tuneUI@315 clearPreview@386 sitePanel@392 draftForm@487 waitDraft@511 skippedText@559 flash@560 wrappers@563 apply@564 startSession@582 renderBar@594 endSession@633 doKeep@639 doDiscard@651 doMore@660 toast@672
#### tryon/server.mjs  (207 lines, js)
  | server.mjs — the try-on helper. ONE local process, no agent polling, no LLM per click:
  |   http://127.0.0.1:<port>  ->  reverse proxy of the dev server (HMR websockets included)
  |                                with the overlay injected into every HTML page;
  |   /__dh/overlay.js           the picker UI;
  |   /__dh/api/*                inspect · open · show · keep · discard · more · save · state · draft ·
  |                              draft-status · tune-* · theme-* (token-gated; the engine does every write).
  | AI drafts: the owner's request is written to .deckhand/tryon/drafts/ and printed on stdout as ONE
  | JSON line ({"event":"draft_request",…}) for an agent watching this process; the overlay polls only
  | the local draft-status endpoint (no model involved) until the agent's gated draft appears.
  | Security: binds loopback only; API calls need the per-run token header (a cross-site page can
  | neither read it nor send the custom header without a CORS preflight, which is never granted).
  deps: tryon/lib/catalog.mjs, tryon/lib/draft.mjs, tryon/lib/engine.mjs, tryon/lib/library.mjs, tryon/lib/project.mjs, tryon/lib/sitetheme.mjs, tryon/lib/slots.mjs, tryon/lib/tune.mjs
  used-by: tryon/cli.mjs, tryon/test/setup-theme-server.test.mjs
  api detectTarget(root)@44
  api startServer({ root: rootIn, port = 3999, target, host = '127.0.0.1', log = () => {}, onDraft = () =>…)@62
  int: probe@34 readBody@53
  const: HERE@32
#### tryon/vite.mjs  (29 lines, js)
  | Deckhand try-on — Vite plugin (dev server only). Usage in vite.config:
  |   import dhTryon from '<skill>/tryon/vite.mjs'
  |   plugins: [dhTryon(), react()]
  | `tryon setup` writes this for you (journaled; `tryon clean` removes it).
  deps: tryon/lib/stamp.cjs
  api dhTryon(opts = {})@13
#### ops/scripts/b2_setup.py  (97 lines, py)
  | b2_setup.py — B2 bucket + lifecycle + scoped app key via the B2 Native API. LIVE-VERIFIED 2026-09-20.
  | Usage:  python3 b2_setup.py <bucket-name> <key-name> [region]  (region default: $B2_REGION or us-east-005)
  | Reads the master key from ~/.vps-ops/secrets/backup.env.sh (B2_MASTER_KEY_ID/_KEY).
  | Idempotent-ish: existing bucket is reused (lifecycle re-applied); a fresh scoped key is minted.
  | Writes scoped creds to ~/.vps-ops/secrets/b2-scoped.env.sh (prints NOTHING secret to stdout).
  | v4 API note: b2_create_key takes `bucketIds` (a LIST) — the old `bucketId` returns 400.
  api load(path)@17
  api api(url, token=None, payload=None, basic=None, raw_token=None)@26
  api main()@47
  const: VAULT@13 OUT@14
#### ops/scripts/backup_verify.py  (137 lines, py)
  | backup_verify.py — the honest proof for Coolify backups (ref 55 §3): LIST THE BUCKETS.
  | B2 via the Native API (scoped key), Tigris via S3 SigV4 ListObjectsV2 (region auto).
  | A "success" execution is a claim; the object is the proof (and the executions endpoint
  | itself can be empty for on-demand runs in v4.3.23).
  | Usage:  python3 backup_verify.py <db_uuid> <tigris_bucket> [--wait 150]
  | Reads the vault: ~/.vps-ops/secrets/{env.sh,backup.env.sh,b2-scoped.env.sh}
  | Exit: 0 = every leg produced evidence · 1 = a leg failed or came back empty (a green exit here is
  | earned by objects in the buckets, never by the script merely finishing — live-hit 2026-09-21).
  api load(path)@19
  api call(method, path, payload=None)@34
  api b2_list()@44
  api tigris_list(bucket)@60
  api main()@88
  const: BASE@16 E@28 TOK@31
#### ops/scripts/coolify_api.py  (467 lines, py)
  | vps-ops — thin Coolify REST client (stdlib only).
  | Config precedence: --url/--token flags > COOLIFY_URL/COOLIFY_TOKEN env
  | > ~/.vps-ops/config.json > token fallback in ~/.vps-ops/secrets/env.sh.
  | See references/10-bootstrap-vps.md for setup, references/40-change-pipeline.md for usage.
  api resolve(url_override=None, token_override=None, env=None, home=None)@26 — Resolve Coolify URL + token, or raise SystemExit('config error: ...').
  api build_request(url, token, method, path, params=None, body=None)@56
  api api(url, token, method, path, params=None, body=None, timeout=60, http=None)@77
  api wait_for_deploy(url, token, uuid, timeout=900, interval=10, api_fn=None, sleep=…, now=…, log=…, expect_commit=None)@106 — Poll the newest deployment for <uuid> until terminal status. Returns 0/3/5.
  api smoke(target, expect=200, contains=None, timeout=30, http=None)@151
  api tunnel_settings(env=None, home=None)@175 — Resolve the SSH tunnel target from env > ~/.vps-ops/config.json. None if unset.
  api tunnel_command(host, key, known_hosts, local_port=8000, remote_port=8000)@204
  api ensure_tunnel(port=8000, log=…, wait=20, spawn=True, settings=None)@230 — 0 when 127.0.0.1:<port> answers (auto-starting the tunnel when configured), else 4.
  api cmd_health(a, url, token)@260
  api cmd_apps(a, url, token)@267
  api cmd_app(a, url, token)@277
  api cmd_deploy(a, url, token)@286
  api cmd_deployments(a, url, token)@298
  api cmd_wait(a, url, token)@312
  api cmd_logs(a, url, token)@317
  api cmd_dlogs(a, url, token)@329
  api cmd_envs(a, url, token)@367
  api cmd_envset(a, url, token)@377
  api cmd_status(a, url, token)@390
  api cmd_smoke(a)@404
  api cmd_tunnel(a, url, token)@408
  api main(argv=None)@416
  int: _http_req@69 _deployment_rows@87 _http_url@142 _url_port@168 _health_ok@212 _spawn_detached@220
  const: HOME@19 OK_STATUS@22 FAIL_STATUS@23
#### ops/scripts/coolify_backup_setup.py  (96 lines, py)
  | coolify_backup_setup.py — Coolify S3 storages + DB backup schedules (ref 55 §0/§1). LIVE-VERIFIED.
  | Reads:  ~/.vps-ops/secrets/env.sh            (COOLIFY_TOKEN, COOLIFY_URL — single-quoted!)
  |         ~/.vps-ops/secrets/backup.env.sh     (Tigris key pair, optional TIGRIS_BUCKET)
  |         ~/.vps-ops/secrets/b2-scoped.env.sh  (B2 scoped key — run scripts/b2_setup.py first)
  | Usage:  python3 coolify_backup_setup.py <db_uuid> [--schedules]
  | Exit: 0 = every storage/schedule leg succeeded · 1 = any leg failed — check it (live-hit 2026-09-21:
  | this script exited 0 no matter what).
  api load(path)@16
  api call(method, path, payload=None)@31
  api ensure_storage(name, endpoint, bucket, region, key, secret)@41
  api main()@58
  const: BASE@13 E@25 TOK@28
#### ops/scripts/hostinger_api.py  (262 lines, py)
  | vps-ops — thin Hostinger API client (stdlib only).
  | Token: HOSTINGER_API_TOKEN env or --token. Base URL: https://developers.hostinger.com
  | Schemas pinned from the official OpenAPI spec (github.com/hostinger/api).
  api build_request(token, method, path, params=None, body=None)@21
  api a_entries(names, ip, ttl=14400)@66
  api update_payload(names, ip, ttl=14400)@70
  api ensure_key(token, key_material, vm_id, name='vps-ops', http=None)@76 — Idempotent: register the key on the account if missing, then attach it to the VM. 0 = ok.
  api dns_set_a(token, domain, ip, names, ttl=14400, http=None)@109 — Validate then PUT the A records (overwrite=true scoped). 0 = ok.
  api firewall_ensure(token, vm_id, name='vps-ops', ports=…, http=None)@125 — Idempotent: create the firewall group if missing, add accept rules, sync, activate. 0 = ok.
  api run_vm(token, a)@159
  api main(argv=None)@193
  int: _http_call@34 _items@57
  const: BASE@17 DEFAULT_KEY_FILE@18
#### ops/scripts/repo_presence.py  (232 lines, py)
  | repo_presence.py — render an app's public face from ONE filled JSON.
  | Takes a filled copy of ``templates/repo-presence/repo.example.json`` and writes:
  | - ``README.md``   from ``README.template.md`` — sections with no content are dropped,
  |                   placeholders that never got data fail loudly (no half-READMEs).
  | - ``LICENSE``     proprietary notice; when ``license_derived_from`` is set, the
  |                   upstream MIT notice is retained at the bottom (compliance).
  | - ``package.json`` metadata only — ``name`` / ``version`` / ``description`` /
  |                   ``license`` get set; everything else is left untouched.
  | It also prints the exact GitHub commands (description, topics, set-default) and
  | with ``--gh`` runs them (and, when repo.json carries enough info, tells you the
  | release command to finish the pass).
  | Usage:
  |   py scripts/repo_presence.py init repo.json [--dir <project>] [--dry-run] [--gh]
  |   python3 scripts/repo_presence.py init repo.json
  | Exit codes: 0 ok · 2 bad input (missing field, leftover placeholder, bad license mode).
  | Style rules this enforces (no fluff): facts only, present tense, one line per
  | feature — never a list of internal details, never a promise the product doesn't keep.
  api fail(msg)@61
  api load_data(path)@66
  api render_readme(data)@85
  api render_license(data)@111
  api patch_package_json(path, data)@131
  api gh_commands(data)@155
  api run_gh(data)@167
  api cmd_init(a)@187
  api main(argv=None)@219
  int: _stringify@79
  const: TEMPLATES@34 REQUIRED@36 MIT_TEXT@38
#### ops/scripts/tigris_bucket.py  (82 lines, py)
  | tigris_bucket.py — create a Tigris bucket the SAFE way and PROVE it serves reads.
  | Why: a console-created bucket can be GLACIER class — it lists objects (HEAD shows the
  | right size) but every GET >~1 KB returns HTTP 200 with 0 bytes to every client
  | (rclone "unexpected EOF", python IncompleteRead). API-created buckets default to
  | STANDARD. `verify` proves readability with a 512 KB round-trip. LIVE-VERIFIED 2026-09-20.
  | Usage:
  |   TIGRIS_ACCESS_KEY_ID=tid_… TIGRIS_SECRET_ACCESS_KEY=tsec_… python3 tigris_bucket.py ls
  |   ... create <bucket>
  |   ... verify <bucket>        # 512 KB put → get → compare → delete
  api call(req)@44
  api main()@52
  int: _req@22
  const: HOST@17 REGION@17 AK@18 SK@19
#### templates/bots/link_audit.py  (70 lines, py)
  | link_audit.py — weekly crawl: broken internal links, pages without <title>/meta description,
  | missing robots.txt / sitemap.xml. Env: SITE_URLS (first = start), MAX_PAGES (150). stdlib only.
  api class Page@15
  api fetch(u)@29
  api main()@38
#### templates/bots/notify.py  (54 lines, py)
  | notify.py — one message, any channel (stdlib). Channel from env: TELEGRAM_BOT_TOKEN+TELEGRAM_CHAT_ID,
  | DISCORD_WEBHOOK_URL, NOTIFY_WEBHOOK_URL (JSON {"text"}), or SMTP_URL (smtp[s]://user:pass@host:port/to@addr).
  | Deduplication: send() only when the message differs from the last one sent with the same key.
  api deliver(text, title='deckhand')@18
  api send(key, text, title='deckhand')@46
  int: _post@13
  const: STATE@10
#### templates/bots/watchdog.py  (111 lines, py)
  | watchdog.py — is the business up? (stdlib; runs anywhere: GitHub Actions, the VPS cron, a laptop)
  | Env: SITE_URLS (comma list, required) · SSL_WARN_DAYS (14) · SLOW_MS (4000)
  |      COOLIFY_URL + COOLIFY_TOKEN (optional: container health + newest backup age, BACKUP_MAX_HOURS=30)
  |      VPS_SSH=user@host (optional: disk usage via ssh, DISK_WARN_PCT=85) · a notify channel (see notify.py)
  | Reports only CHANGES (down, recovered, new warning) — never the same alert twice. Exit 1 while something is down.
  api http_check(url, slow_ms)@20
  api ssl_days(host)@33
  api coolify(url, token, max_hours)@43
  api disk(target, warn)@75
  api main()@84
#### scripts/leak_sweep.py  (245 lines, py)
  | leak_sweep.py — the release gate for this (public) repo.
  | Scans for private-project terms before anything is pushed or released:
  | - the repo working tree (default: this script's repo root),
  | - every ``*.db`` tracked by git, opened read-only: ``registry='mine'`` rows and
  |   the TEXT columns of ``registry_items`` / ``templates`` (library_import.py writes the
  |   owner's free-text ``--source`` into a git-tracked .db, which the text tree walk skips),
  | - the commit messages of the repo's branches and tags,
  | - every harness copy of the pack skills (hermes install + the .claude/.agents/.codex mirrors),
  | - any extra files passed with --file (a release-notes draft — that is the exact moment
  |   wordings leak in) and directories passed with --dir.
  | Terms come from a private, never-published list: ``~/.deckhand/private-terms.txt``
  | (one per line, '#' comments allowed). If the list is missing, the gate FAILS (exit 2)
  | rather than passing silently — a gate that cannot check is not a gate.
  | Usage:
  |   py scripts/leak_sweep.py [--repo DIR] [--terms FILE] [--file NOTES.md]... [--dir DIR]...
  | Exit: 0 clean · 1 hits · 2 setup problem (missing terms list, bad path).
  api fail(msg)@39
  api load_terms(path)@44
  api is_text(path)@56
  api terms_in(text, terms)@66
  api scan_tree(root, terms, label)@71
  api sweep_sqlite(path, terms)@95 — Gate one SQLite db, opened read-only (``mode=ro`` — nothing is ever written).
  api scan_tracked_dbs(repo, terms)@151 — Sweep every ``*.db`` in ``git ls-files`` (read-only, see sweep_sqlite).
  api scan_history(repo, terms)@168
  api main(argv=None)@190
  const: PACK_SKILLS@32 SKIP_DIRS@33 SKIP_EXT@34
#### scripts/llm_context.py  (999 lines, py)
  | llm_context.py — builds LLM_CONTEXT.md: this whole repo, compressed for a model that has no memory.
  |   python3 scripts/llm_context.py            regenerate LLM_CONTEXT.md from the working tree
  |   python3 scripts/llm_context.py --check    exit 1 when LLM_CONTEXT.md is stale or a curated anchor broke
  |   python3 scripts/llm_context.py --index    regenerate from the git index (what .githooks/pre-commit runs)
  | Two inputs. (1) The code: every [gen] section is extracted from the files themselves (tree, commands,
  | modules with exact line numbers, error codes, env vars, data shapes, docs outline, tests, CI), so it cannot
  | drift. (2) scripts/llm_context.core.md: the curated model (architecture, invariants, flows, routing,
  | gotchas). The core points at code through anchors, resolved to path:line at build time:
  |   [[path]]  [[path#symbol]]  [[dh:subcommand]]  [[tryon:command]]  [[api:name]]  [[!ERROR_CODE]]
  | An anchor that no longer resolves fails the build, so the curated half cannot silently rot either.
  | Deterministic (same tree -> same bytes), stdlib only, Python 3.9+.
  api load_tree(index=False)@49 — {repo-relative posix path: text or None (binary)} — the git index, or what `git add -A` would commit.
  api decode(path, b)@84 — Text with LF line ends (a Windows checkout gives the same map), or None for a binary file.
  api short(path)@91 — Display path: skill files relative to skills/deckhand/, everything else relative to the repo root.
  api full(path, files)@96
  api one(s, cap=…)@103
  api collapsed(path)@108
  api py_sig(fn)@123
  api py_info(path, text)@138
  api js_info(path, text)@230
  api md_info(text)@295
  api analyze(files)@307
  api purpose(path, text, info)@325
  api const_values(files, pkg, node)@362 — Values of MODULE.NAME for an `from . import module as ALIAS` constant (literal, or [x["k"] for x in LITERAL]).
  api dh_commands(files, mods)@385 — (commands, handlers) from dhlib/cli.py: argparse structure + the dispatch branch of each command.
  api js_cases(text, fn_start)@474 — [(case, line, body)] for the switch inside the function whose definition line contains fn_start.
  api js_imports(path, text)@497 — local name -> (module path, exported name or '*').
  api js_calls(body, imports)@515
  api tryon_commands(files)@525
  api api_routes(files)@537
  api is_test(path)@559
  api scan_index(files, patterns, code_only=True)@563
  api shape(x, depth=0)@577
  api data_notes(path, d)@587 — Extra, content-level facts for the data files an agent asks about most.
  api class Anchors@614
  api sec_map(files, mods)@669
  api sec_commands(files, mods, dh, handlers, tryon, api)@700
  api sec_modules(files, mods, used_by, curated)@736
  api sec_errors(errors)@771
  api sec_env(env)@782
  api sec_data(files)@789
  api sec_docs(files, mods)@821
  api sec_tests(files, mods)@837
  api sec_ci(files)@874
  api build(files)@891
  api main(argv=None)@962
  int: _git@45 _default@117 _params@196 _js_doc@216 _loc@694
  const: ROOT@28 OUT@29 CORE@30 SKILL@31 BINARY_EXT@32 CODE_EXT@33 COLLAPSE@35 CAP@40 JS_HEADER@181 JS_DEF@182 JS_FN_RHS@191 JS_ANY_DEF@192 JS_IMPORT@193 ERR_PATTERNS@546 ENV_PATTERNS@552
#### scripts/version_check.py  (42 lines, py)
  | version_check.py — every human-readable version claim must match: README badge ==
  | skills/deckhand/SKILL.md `version:` == the top CHANGELOG.md entry (== --tag vX.Y.Z when given).
  | Exit 0 = consistent · 1 = mismatch · 2 = usage/IO error. Stdlib only.
  api main(argv=None)@11

## §10 ERRORS [gen] — code → where raised

`CODE  where (file:line) — message start`. dh prints {ok:false, code, message}; tryon prints {ok:false, code, message}; the helper API returns the same with HTTP 4xx.

- BAD_COLOR  dhlib/brand.py:108 — brand.primary {color!r}: use #hex, rgb(), hsl() or oklch()
- BAD_GATE  dhlib/state.py:147 — gate must be one of {list(GATES)}
- BAD_KIND  dhlib/plan.py:315 — kind must be one of {BB_KINDS}
- BAD_MODE  dhlib/state.py:57 — mode must be one of {MODES}
- BAD_NAME  dhlib/profile.py:109, tryon/lib/library.mjs:32 — vault names are UPPER_SNAKE_CASE
- BAD_PAIR  dhlib/cli.py:25, dhlib/profile.py:80 — expected key=value, got {p!r}
- BAD_PATH  dhlib/state.py:59 — path must be one of {PATHS}
- BAD_PHASE  dhlib/state.py:110 — phase must be one of {PHASE_IDS}
- BAD_REPO  dhlib/pool.py:187 — expected owner/name or a github.com URL
- BAD_RUNG  dhlib/learn.py:65 — rung: {RUNGS}
- BAD_RUNNER  dhlib/ops.py:50 — runner: github | cron | local
- BAD_SIGNATURE  dhlib/learn.py:70 — signature is not a valid regex: {e}
- BAD_TOKEN  tryon/server.mjs:172
- CHECK_FAILED  dhlib/cli.py:122 — phase {a.phase} is not done yet
- CLONE_FAILED  dhlib/build.py:96, dhlib/build.py:120, dhlib/github.py:94
- COMMAND_FAILED  dhlib/cli.py:261, dhlib/util.py:119 — exit {r[
- COMPOSE_FAILED  dhlib/build.py:168
- CONTENT_LESS_ITEM  tryon/lib/registry.mjs:145
- CREDENTIAL  tryon/lib/library.mjs:35
- DEPLOY_FAILED  dhlib/deploy.py:91 — deployment {d[
- DEPLOY_REJECTED  dhlib/deploy.py:85 — HTTP {st} {str(body)[:300]}
- DEST_NOT_EMPTY  dhlib/build.py:83, dhlib/build.py:138, dhlib/harvest.py:35 — {to} is not empty — pick a new folder
- DIRTY_TREE  dhlib/deploy.py:75 — commit first — Coolify builds what is pushed, not what is on disk
- DRAFT_DONE  tryon/lib/draft.mjs:245 — draft ${id} is already on the page (session ${d.session})
- DRAFT_REJECTED  tryon/lib/draft.mjs:252 — the AI draft failed ${g.problems.length} check(s) — fix them in ${d.brief.write…
- ELEMENT_NOT_FOUND  tryon/lib/draft.mjs:89, tryon/lib/engine.mjs:208, tryon/lib/engine.mjs:901, tryon/lib/tune.mjs:179 — no JSX element starts at ${relFile}:${line}:${col}
- EMPTY  dhlib/profile.py:113 — no value given
- FILE_CHANGED  tryon/lib/tune.mjs:193, tryon/lib/tune.mjs:219
- GATE_BLOCKED  dhlib/state.py:117 — gate {g} ({GATES[g]}) needs the owner
- GENERATED_FILE  tryon/lib/draft.mjs:80, tryon/lib/engine.mjs:202
- GITHUB_AUTH  dhlib/github.py:53 — GitHub rejected the token (HTTP {st}): {d.get(
- GITHUB_CREATE  dhlib/github.py:67 — could not create {full} (HTTP {st}): {d.get(
- GITHUB_PUSH  dhlib/github.py:86
- GITHUB_READ  dhlib/github.py:103 — {full}/{path}: HTTP {st} {d.get(
- GITHUB_WRITE  dhlib/github.py:113 — {full}/{path}: HTTP {st} {d.get(
- INSIDE_VARIANT  tryon/lib/draft.mjs:81, tryon/lib/engine.mjs:203 — that element belongs to a variant being tried — keep or discard it first
- INSTALL_FAILED  tryon/lib/engine.mjs:320
- INTERRUPTED  dhlib/cli.py:313
- LEAKS  dhlib/cli.py:202 — {r[
- LICENCE_REFUSED  dhlib/build.py:80 — {name}: licence {row.get(
- NEED_REASON  dhlib/state.py:134 — a skipped phase needs --reason (it is shown to the owner)
- NOT_KEPT  tryon/lib/library.mjs:53
- NO_BRAND  dhlib/brand.py:78 — brand.name missing (dh brief set brand.name=\
- NO_CANDIDATES  tryon/lib/engine.mjs:221 — no licensed ${slot} candidates for a ${prof.base} project
- NO_CONFIG_EXPORT  tryon/lib/setup.mjs:97
- NO_COOLIFY  dhlib/deploy.py:30
- NO_DEV_SERVER  tryon/cli.mjs:60 — start the dev server first (npm run dev), or pass --target http://127.0.0.1:<po…
- NO_DRAFT  tryon/lib/draft.mjs:43 — no AI draft request
- NO_FAILURE  dhlib/learn.py:170 — no recorded failure (run commands through `dh run -- …`)
- NO_FILE  tryon/lib/draft.mjs:82, tryon/lib/engine.mjs:898
- NO_GITHUB_TOKEN  dhlib/github.py:26 — ACTION NEEDED — create a fine-grained GitHub token (github.com → Settings → Dev…
- NO_GLOBALS_CSS  tryon/lib/sitetheme.mjs:238
- NO_LICENCE  tryon/lib/library.mjs:30
- NO_NODE  dhlib/build.py:160, dhlib/cli.py:275 — node is required for compose
- NO_PLUGINS_ARRAY  tryon/lib/setup.mjs:120
- NO_PORT  dhlib/build.py:177 — no free port in {start}-{start + 49}
- NO_RUN  dhlib/state.py:42 — no .deckhand/run.json in {root} — start with `dh init`
- NO_SESSION  tryon/lib/engine.mjs:55 — no try-on session
- NO_SITEMAP  dhlib/plan.py:210 — no .deckhand/sitemap.json
- NO_SLOT  tryon/lib/draft.mjs:86, tryon/lib/engine.mjs:210 — say what this element is (hero, pricing, button…)
- NO_SOURCE  dhlib/autopsy.py:160, dhlib/autopsy.py:607 — no session found: pass a transcript path, or run commands through `dh run -- …`…
- NO_SUCH_API  tryon/server.mjs:122
- NO_SUCH_BOT  dhlib/ops.py:48
- NO_SUCH_PATH  dhlib/build.py:124
- NO_SUCH_TEMPLATE  dhlib/build.py:78, dhlib/pool.py:128 — {name} is not in the pool (dh pool query …)
- NO_TARGET  dhlib/deploy.py:72 — first deploy is the runbook (references/ops/30-deploy-app.md), then `dh deploy…
- NO_TUNE  tryon/lib/tune.mjs:167
- NO_UNDO  tryon/lib/sitetheme.mjs:262
- NO_URL  dhlib/deploy.py:51 — no live URL (dh deploy target --url https://…)
- NO_VARIANT  tryon/lib/engine.mjs:798 — session ${id} has no variant ${i}
- NO_VARIANTS  tryon/lib/engine.mjs:305, tryon/lib/engine.mjs:317 — no candidate could be staged
- NO_VITE_CONFIG  tryon/lib/setup.mjs:143
- OUTSIDE_PROJECT  tryon/lib/draft.mjs:79, tryon/lib/engine.mjs:201, tryon/lib/tune.mjs:176
- OUT_OF_ORDER  dhlib/state.py:114 — phase
- PLAN_ERRORS  dhlib/cli.py:170 — {len(r[
- PLAN_NOT_CLEAN  dhlib/plan.py:252 — fix the plan first (dh plan lint)
- PUSH_FAILED  dhlib/deploy.py:79
- REFUSED  dhlib/pool.py:239, tryon/lib/vet.mjs:93 — {row.get(
- REPO_NOT_EMPTY  dhlib/harvest.py:96 — {full} already has commits — pick another --repo, or delete it first
- SCAFFOLD_FAILED  dhlib/build.py:143
- SECRETS_IN_BASE  dhlib/harvest.py:89 — not pushed: credential-shaped values are in the base — remove them, then harves…
- SECRET_IN_PROFILE  dhlib/profile.py:83 — {k} looks like a secret — use `dh vault set {k.upper()}` instead
- SESSION_CLOSED  tryon/lib/draft.mjs:73, tryon/lib/engine.mjs:391, tryon/lib/engine.mjs:794, tryon/lib/engine.mjs:861 — session ${session} is ${s.state}
- SMOKE_FAILED  dhlib/cli.py:227, dhlib/deploy.py:94 — deployed, but the live check failed — roll back per references/ops/40-change-pi…
- SWAP_INCOMPLETE  dhlib/cli.py:210 — a vendor SDK is still in the code
- TOO_MANY_FILES  tryon/lib/registry.mjs:121
- TUNE_CLOSED  tryon/lib/tune.mjs:191
- UNKNOWN_SOURCE  dhlib/autopsy.py:146 — {path}: not a Claude Code transcript nor a deckhand/plain run log
- UNSUPPORTED_FRAMEWORK  tryon/lib/setup.mjs:146 — degraded mode:
- USAGE  dhlib/cli.py:140, dhlib/cli.py:157, dhlib/cli.py:179, dhlib/cli.py:237, dhlib/cli.py:244, dhlib/cli.py:280, dhlib/learn.py:125, tryon/cli.mjs:53, tryon/cli.mjs:127, tryon/cli.mjs:152, tryon/lib/vet.mjs:34 — dh vault set NAME (value on stdin or prompted — never on the command line)
- VERIFY_FAILED  dhlib/cli.py:216 — blocking rows are red (see .deckhand/VERIFY.md)
- WRAPPER_MISSING  tryon/lib/engine.mjs:396, tryon/lib/engine.mjs:802 — the variant wrapper is gone from

## §11 ENV [gen] — variables read

Environment variables read by the code (tests set more; see §TESTS files).

- B2_REGION  ops/scripts/b2_setup.py:49
- BACKUP_MAX_HOURS  templates/bots/watchdog.py:99
- BOT_STATE_DIR  templates/bots/notify.py:10
- COOLIFY_TOKEN  dhlib/deploy.py:101, templates/bots/watchdog.py:98, templates/bots/watchdog.py:99
- COOLIFY_URL  dhlib/deploy.py:101, dhlib/deploy.py:25, dhlib/profile.py:147, ops/scripts/backup_verify.py:16, ops/scripts/coolify_backup_setup.py:13, templates/bots/watchdog.py:98, templates/bots/watchdog.py:99
- DECKHAND_HOME  dhlib/util.py:22, tryon/lib/catalog.mjs:16, tryon/lib/registry.mjs:26
- DECKHAND_LIBRARY  tryon/lib/catalog.mjs:16
- DECKHAND_TRYON  tryon/lib/setup.mjs:35
- DH_CACHE  tryon/lib/registry.mjs:26
- DH_DEBUG  tryon/compose.mjs:103, tryon/compose.mjs:104
- DH_DEBUG_BAKE  tryon/lib/engine.mjs:679
- DH_FIXTURES  tryon/lib/registry.mjs:33
- DH_FIXTURES_PASSTHROUGH  tryon/lib/registry.mjs:40
- DH_GITHUB_API  dhlib/github.py:19
- DH_GITHUB_GIT  dhlib/github.py:20
- DH_RECORD  tryon/lib/registry.mjs:57
- DISCORD_WEBHOOK_URL  templates/bots/notify.py:24, templates/bots/notify.py:25
- DISK_WARN_PCT  templates/bots/watchdog.py:101
- GITHUB_TOKEN  dhlib/github.py:24
- HOSTINGER_API_TOKEN  ops/scripts/hostinger_api.py:226
- LOCALAPPDATA  scripts/leak_sweep.py:209
- MAX_PAGES  templates/bots/link_audit.py:48
- NODE_ENV  tryon/lib/setup.mjs:35, tryon/loader.cjs:13
- NOTIFY_WEBHOOK_URL  templates/bots/notify.py:27, templates/bots/notify.py:28
- SITE_URLS  templates/bots/link_audit.py:39, templates/bots/watchdog.py:85
- SLOW_MS  templates/bots/watchdog.py:90
- SMTP_URL  templates/bots/notify.py:30, templates/bots/notify.py:31
- SSL_WARN_DAYS  templates/bots/watchdog.py:96
- TELEGRAM_BOT_TOKEN  templates/bots/notify.py:20, templates/bots/notify.py:21
- TELEGRAM_CHAT_ID  templates/bots/notify.py:20, templates/bots/notify.py:22
- TIGRIS_ACCESS_KEY_ID  ops/scripts/tigris_bucket.py:18
- TIGRIS_SECRET_ACCESS_KEY  ops/scripts/tigris_bucket.py:19
- TURBOPACK  tryon/lib/setup.mjs:49
- VPS_OPS_HOME  ops/scripts/coolify_api.py:19
- VPS_SSH  templates/bots/watchdog.py:100, templates/bots/watchdog.py:101

## §12 DATA [gen] — shipped data + templates

Shape legend: {key:type} · [N×item] · s string · n number · b bool. Query data through commands (dh pool query, tryon query) instead of reading it.

- data/bots.json  {version:n, note:s, bots:[11×{…}]}
    bots: watchdog(generic), link-audit(generic), backup-proof(generic), deps-audit(generic), weekly-kpi(app), lead-digest(app), abandoned-signup(app), payment-failed(app), booking-reminder(app), review-request(app), low-stock(app)
- data/components.index.json  {version:n, built:s, count:n, items:[738×{…}]}
    by registry: tailark-oss 300, shadcn 197, smoothui 113, magicui 57, basecn 36, kokonutui 35
    by kind: block 399, ui 251, effect 88
- data/lessons.seed.jsonl  jsonl 20 rows × {id:s, phase:s, signature:s, symptom:s, cause:s, fix:s, rung:s, stack:[1×s], seen:n, source:s, command:s}
    S-001 [build/preflight] EACCES on port 5432 (Windows) → pin another port (5770) in the db script, DATABASE_URL and the test config together
    S-002 [build/eliminate] npm script written with single quotes breaks on cmd.exe → use escaped double quotes in package.json scripts
    S-003 [build/pitfall] spawn npm ENOENT from a tool that spawns without a shell → point the tool at a real executable (node node_modules/.../bin.js) or resolve with shutil…
    S-004 [build/eliminate] buttonVariants() called from a server component → drop 'use client' from the primitive file (it has no hooks)
    S-005 [build/eliminate] Turbopack: Can't resolve './x.tsx.tsx' → remove `as` from same-type loader rules (deckhand's stamp rule has none)
    S-006 [tryon/eliminate] Next dev blocks HMR/dev resources through a proxy → proxy rewrites Host/Origin to localhost (tryon serve does this)
    S-007 [tryon/eliminate] next/image: remote demo image host not configured → staged <Image> gets `unoptimized`; demo photos are replaced by /deckhand-placeholder.svg
    S-008 [tryon/eliminate] TypeScript 7 has no JS compiler API → deckhand parses with vendored @babel/parser — never the project's typescript
    S-009 [tryon/eliminate] upstream SVGProps without type argument → materialize compat pass adds the type argument
    S-010 [tryon/eliminate] tailark.com registry blocks answer 401 → use the MIT OSS source (tailark/blocks on GitHub, oss-tailark.com)
    S-011 [tryon/eliminate] cult-ui registry answers 429 (bot challenge) → excluded from the catalog; do not retry
    S-012 [deploy/eliminate] Next warns Unrecognized key 'turbo' → the next-plugin picks the right home from next's version
    S-013 [deploy/gate] Coolify dashboard reachable from the internet → loopback-bind Coolify's ports in its compose + SSH tunnel (references/ops/10-bootstrap-vp…
    S-014 [deploy/eliminate] a stale deployment looked like success → wait with --expect-commit <sha> (dh deploy ship does)
    S-015 [deploy/preflight] ssh: Host key verification failed in an unattended run → store the host key at first contact in the durable known-hosts path (references/ops/10-bo…
    S-016 [operate/gate] a schedule that was never verified → fire it once through its own trigger (gh workflow run / systemctl start / schtasks /Run)…
    S-017 [deploy/preflight] Prisma P1001 can't reach the database in Coolify → use the Coolify internal URL of the database (postgres://…@<db-uuid>:5432/…)
    S-018 [deploy/preflight] auth secret missing at build/start → generate a local secret per environment (dh clone does for .env; set it in Coolify env fo…
    S-019 [build/preflight] pnpm install --frozen-lockfile fails → run pnpm install locally, commit the lockfile, then deploy
    S-020 [review/pitfall] next build: Dynamic server usage error → export const dynamic = 'force-dynamic' on that route, or move the read into a server acti…
- data/licenses.json  {_about:s, accepted:{MIT:s, Apache-2.0:s, BSD-2-Clause:s, BSD-3-Clause:s, ISC:s, 0BSD:s, Unlicense:s}, refused:{AGPL-3.0:s, GPL-2.0:s, GPL-3.0:s, LGPL-2.1:s, LGPL-3.0:s, MPL-2.0:s, EPL-2.0:s, SSPL-1.0:s, BUSL-1.1:s, Elastic-2.0:s, CC-BY-NC-4.0:s, CC-BY-SA-4.0:s, NOASSERTION:s, OTHER:s, NONE:s}, package_license_refused:[4×s], paid_packages:[15×s], paywall_text:s, secret_files:s}
    accepted: MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, 0BSD, Unlicense · refused: AGPL-3.0, GPL-2.0, GPL-3.0, LGPL-2.1, LGPL-3.0, MPL-2.0, EPL-2.0, SSPL-1.0, BUSL-1.1, Elastic-2.0, CC-BY-NC-4.0, CC-BY-SA-4.0, NOASSERTION, OTHER, NONE
- data/pool.json  {version:n, policy:s, count:n, templates:[12×{…}]}
    rows: open-saas(saas,MIT), next-js-boilerplate(saas,MIT), full-stack-fastapi-template(None,MIT), micro-saas-starter-kit(saas,MIT), next-elite(saas,MIT), epic-stack(None,MIT), nextjs-saas-starter(saas,MIT), next-saas-stripe-starter(saas,MIT), bagisto(catalogue,MIT), cal-ai-tutorial(None,MIT), full-stack-ai-agent-template(saas,MIT), triply-ai(None,MIT)
- data/registries.json  {version:n, updated:s, policy:s, refused:[6×{…}], registries:[6×{…}]}
    indexed: tailark-oss(MIT), shadcn(MIT), magicui(MIT), kokonutui(MIT), smoothui(MIT), basecn(MIT)
    refused: coss (originui), animate-ui, aceternity, 21st.dev, tailark.com (pro), cult-ui
- templates/brief.json  {name:s, business:s, shape:s, audience:s, market:s, languages:[1×s], brand:{name:s, tagline:s, primary:s, logo:s, tone:s}, features:[0×], money:s, content:s, hosting:s, domain:s, deadline:s, assumed:[0×]}
- templates/ops/repo-presence/repo.example.json  {repo:s, name:s, tagline:s, live_url:s, version:s, intro:s, features:[2×s], stack:s, setup_commands:[4×s], env_note:s, env_table:[2×s], layout:[3×s], ci_line:s, deploy_notes:s, status:s, package_json:b, …}
- templates/research.json  {business_summary:s, audience:{primary:s, secondary:s, language:s, pains:[0×], jobs_to_be_done:[0×]}, competitors:[1×{…}], conversion:{plays:[0×], first_screen:[0×], trust:[0×], objections:[0×]}, discovery:{keywords:[0×], local:b, pages_that_rank:[0×]}, features:{now:[0×], later:[0×], not:[0×]}, unproven:[0×], sources:[0×]}
- templates/sitemap.json  {name:s, nav:{header:[4×s], footer:[2×s]}, pages:[8×{…}], features:[2×{…}], forms:[1×{…}], entities:[2×{…}]}
- data/pool/*.json  12 files, each {repo:s, slug:s, measured_at:s, upstream_commit:s, default_branch:s, archived:b, size_kb:n, languages:{PHP:n, Blade:n, TypeScript:n, JavaScript:n, CSS:n, Dockerfile:n, Shell:n}, framework:{name:s, version:s, router:s, notes:s}, package_manager:s, node_or_python_requirement:s, monorepo:{is_monorepo:b, workspaces:[1×s], notes:s}, stack:{db:s, orm:s, auth:{…}, ui:[6×s], i18n:{…}, payments:{…}, email:{…}, analytics:[0×], jobs:[3×s], search:[3×s], storage:[3×s], realtime:[2×s], flags:[0×], observability:[3×s]}, features:[47×s], routes:{pages:[36×s], api:[28×s], protected:[4×s], count:n, notes:s}, env:{count:n, names:[60×s], required_at_build:[0×], notes:s}, …}

## §13 DOCS [gen] — markdown outline

Every markdown file: `path (lines): # title › ## sections`. Load a reference only when `dh next` names it.

- AGENTS.md (22): # AGENTS.md — Deckhand
- CHANGELOG.md (97): [2.0.0] — 2026-09-25 — Deckhand v2 (full rework)
- README.md (181): ## ☕ The uncomfortable math › ## 🚀 Start in 2 minutes › ## 🧭 What happens when you ask › ## 🪄 The parts nobody else does › ## 🪙 Why it costs so few tokens › ## ✅ Proven, and what isn't yet › ## 🔒 Rules it never breaks › ## 🙏 A note from me › ## ❓ FAQ › ## 📄 License
- RELEASING.md (8): # Releasing
- docs/USE-CASES.md (214): # Use cases — copy a sentence, get a result › ## 1. Idea → live website, zero tech › ## 2. A template I found on GitHub › ## 3. Make my existing site look premium › ## 4. A design no library has › ## 5. Launch for $0, even without a domain › ## 6. Go live on my own domain › ## 7. Keep it running while I sleep › ## 8. Stop the agent repeating the same mistake › ## 9. Reuse this site for my next client › ## 10. For developers
- SKILL.md (134): # Deckhand v2 — operating specification › ## 1. Control plane (read this, then use `dh next`) › ## 2. State machine › ## 3. Invariants (MUST / MUST NOT) › ## 4. Command surface › ## 5. Try-on (live component swap) — zero model calls per click › ## 6. Delegation protocol (parallel agents) › ## 7. Owner communication protocol › ## 8. Harness notes
- references/00-define.md (37): # 00 — DEFINE (the only interview) › ## Procedure › ## Rules
- references/10-research.md (30): # 10 — RESEARCH (evidence, not opinions) › ## Specification › ## Procedure › ## Rules
- references/20-plan.md (39): # 20 — PLAN (the page map is an interaction graph) › ## Schema (`dh plan init` writes a lint-clean example to adapt) › ## Lint (errors block the phase) › ## Procedure › ## Copy
- references/30-build.md (63): # 30 — BUILD (four ways in, one running app) › ## Entry by path › ### copy.json (scratch path) — the only input the model writes: words… › ## Adding a base someone found (vetted, never on trust) › ## Swap rented services (pool/existing bases) › ## Implementing the plan › ## Run it › ## Traps
- references/40-brand.md (20): # 40 — BRAND (their identity in, the template's out) › ## Procedure › ## Rules
- references/50-tryon.md (98): # 50 — TRY-ON (live section/component swap, zero model calls per clic… › ## Operate › ## Tune it (adjust, don't replace) and Site (the whole look) — no mod… › ## No licensed design fits? The AI draft (labelled, gated) › ## A registry the owner found › ## What the engine guarantees › ## Troubleshooting
- references/60-review.md (26): # 60 — REVIEW (one command, evidence rows) › ## Rows › ## Procedure
- references/70-deploy.md (32): # 70 — DEPLOY (proof, not hope) › ## First deploy (once per server/app) — runbooks in `references/ops/` › ## No domain yet ($0, or "just let me see it live") › ## Every release › ## Handoff
- references/80-operate.md (44): # 80 — OPERATE (the business keeps running; the pack keeps learning) › ## Change requests (any time after deploy) › ## Bots and crons › ## The learning loop (autopsy — deterministic, evidence only) › ## Reuse — the owner's own library of bases (on their GitHub)
- references/ops/00-user-checklist.md (169): # 00 — User checklist: the only things we ask the user for › ## 1. What we ask for (and nothing else) › ## 2. Hostinger path (preferred — near-zero user work) › ## 3. Generic path (any other provider) › ## 3b. Free preview track — offered, never forced (refs 11/21) › ## 4. Two one-time browser approvals (agent guides, click-by-click) › ## 5. Business keys matrix — the agent asks only for the rows your ap… › ## 6. What the user will NEVER have to do › ## 7. DNS records (non-Hostinger registrar) — copy-paste block › ## 8. One-paste hand-off
- references/ops/10-bootstrap-vps.md (273): # 10 — Bootstrap: SSH key → key attach → firewall → Coolify → token →… › ## Step 0 — keygen on the agent machine › ## Step 1a — Hostinger: register + attach the key via API › ## Step 1b — generic: one-paste key install › ## Step 2 — verify SSH (gate: do not continue until this passes) › ## Step 3 — firewall BEFORE install › ### Hostinger (preferred — provider-level, installs nothing on the bo… › ### Generic (ufw — non-Hostinger providers only) › ## Step 3b — lock the dashboard: the docker-aware way (live-verified… › ## Step 4 — install Coolify + verify › ## Step 5 — token handoff + storage › ## Step 6 — harden (only after Step 2 proved key auth) › ## Step 7 — golden snapshot (Hostinger) › ## Step 8 — Coolify instance domain (recommended: unlocks HTTPS + MCP) › ## Harness MCP wiring (optional convenience layer) › ## Failure remedies
- references/ops/11-oracle-free-tier.md (136): # 11 — Oracle Cloud Always Free: the $0 preview server (Arm) › ## Facts (verified 2026-09-18) › ## What the user does (browser — the complete list, guided click-by-c… › ## Agent steps › ## Capacity — "Out of host capacity" ladder › ## Arm (aarch64) notes › ## Backups on the free tier › ## Reclamation tail risk (state it honestly in every report) › ## If Oracle signup fails (card rejected, verification stall, capacit… › ## Provenance
- references/ops/12-provider-price-sheet.md (64): # 12 — Provider price sheet (Oracle out? pick from here) › ## Decision order › ## Sheet (2026-09-18) › ## Contabo — live drill notes (Cloud VPS 4 "2026", 2026-09-18) › ## Sizing rules of thumb › ## Provenance
- references/ops/20-domain-dns-ssl.md (162): # 20 — Domain, DNS & SSL: records → Hostinger API (or manual) → Cooli… › ## Record plan › ## Hostinger route (exact API flow) › ### 1. Read first — this is also the backup › ### 2. Write `proposed.json` — A records only › ### 3. Validate (dry run) — must be 200 before the PUT › ### 4. Apply › ### 5. Re-read to verify (never trust the write) › ### Rollback — DNS snapshots › ## Manual registrar route › ## Propagation check (do this BEFORE touching Coolify / SSL) › ## Coolify side — attach the domain to the app › ## Let's Encrypt verification › ## Troubleshooting › ## Next
- references/ops/21-free-domain-cloudflare.md (112): # 21 — Preview domains: .pp.ua (free) or an owned domain → Cloudflare… › ## .pp.ua facts (verified 2026-09-18) › ## A. User steps (browser — complete list, guided click-by-click) › ## B. Agent steps › ## C. Email reality on a preview domain › ## D. Already own a domain? (any registrar — NameSilo, Namecheap, GoD… › ## Alternatives (ranked, 2026) › ## Provenance
- references/ops/30-deploy-app.md (255): # 30 — Deploy an app (repo → Coolify app → envs → Postgres → HTTPS →… › ## 0. Prerequisites on the repo › ## 1. Pin the UUIDs (once per deploy) › ## 2. Create the application › ### 2a. Public repo (fastest) › ### 2b. Private repo — two routes › ## 3. Environment variables › ## 4. Postgres (skip if the app brings its own DB) › ## 5. Attach the domain › ## 6. First deploy + verify › ## 6b. First-run data steps — inside the running container (live-veri… › ## 7. Auto-deploy on push › ## 8. Session anchor — `<project>/.vps-ops.json` › ## 9. Handoff file — `OPS.md` (write it at deploy time, keep it curre… › ## Done checklist
- references/ops/40-change-pipeline.md (206): # 40 — The change pipeline (implement → push → deploy → wait → smoke… › ## The loop › ## Step by step › ## Hard rules › ## Failure classification › ## Rollback (exact) › ## Decision rule: fix-forward vs rollback
- references/ops/50-ops-monitoring.md (138): # 50 — Operate: status, logs, metrics, cleanup, backups, updates, inc… › ## 1. Status at ask-time › ## 2. Logs › ## 3. Server health (Hostinger) › ## 4. Disk reclamation (Coolify) › ## 5. Backups — local dump + restore recipe (**offsite dual-target is… › ## 6. Updates › ## 7. Incident playbook › ## 8. Routine cadence
- references/ops/55-offsite-backups.md (134): # 55 — Offsite backups (never on the same VPS) › ## 0. Prices of entry — human-once, ONE batched ask › ## 0b. Destination choices — ask ONCE, batched (offer, never force) › ## 1. Wire it in Coolify — API, not dashboard › ## 2. Alerts — point notifications at the app's mail chain › ## 3. Verification — the only honest proofs › ## 4. Restore reality (the one thing the API can't do) › ## 5. Deep layer (recommended): restic → both providers › ## 6. Known gaps (v4.3.23) — don't rediscover them live › ## 7. Home copies (pull model — works behind NAT; the VPS is never as…
- references/ops/56-app-object-storage.md (59): # 56 — App object storage (uploads) — RustFS via Coolify › ## Verdict (Sept 2026): **RustFS 1.0.x — Apache-2.0** › ## Deploy (agent-driven) › ## App contract (Next.js) › ## Offsite for the files (two layers) › ## When NOT to use this
- references/ops/60-migrate-to-paid.md (103): # 60 — Migrate: free preview → paid host (Coolify → Coolify) › ## What moves › ## New box, before the switch (T-14 → T-1) › ## T-0 switch (user-visible downtime = the freeze only, ~5–15 min) › ## Rollback matrix › ## Post-migration (same day) › ## Fallbacks (unverified — rehearse before trusting) › ## Provenance
- references/ops/70-auth-and-email.md (142): # 70 — Auth & login email (verify + reset that actually arrives) › ## 1. Auth verdict (Sept 2026, primary-source verified) › ### Better Auth — deploy facts (Coolify / Traefik / Next.js) › ## 2. Email — the free chain (zero budget, Sept 2026 verified) › ## 3. Deliverability DNS — the part that makes mail arrive › ## 4. Failover router — drop-in (`templates/ops/mail-router/`) › ## 5. Smoke test (auth + email) — part of "deployed" › ## 6. Capacity to quote
- references/ops/80-mcp-integrations.md (61): # 80 — MCP integrations (modular catalog) › ## Domain: DNS + registrar › ## Domain: Email › ## Domain: Backup & object storage › ## Domain: payments / analytics / other (append here)
- templates/PENDING.md (8): # PENDING — {{NAME}} › ## Open › ## Done
- templates/ops/OPS.md (112): # OPS.md — <App name> (`<domain>`) › ## TL;DR for an agent › ## What this app is › ## Live surfaces › ## Server + platform › ## Domain + DNS › ## Email (if the app sends mail — ref 70) › ## Secrets inventory — locations only, never values › ## Everyday commands (copy-paste) › ## ⚠️ Do not do these › ## Provenance
- templates/ops/mail-router/README.md (37): # mail-router — multi-provider email failover (drop-in) › ## Install (agent, ~10 min) › ## Better Auth wiring › ## Rules (from ref 70 — don't relax)
- templates/ops/repo-presence/README.md (33): # repo-presence — the one-time "nice repo" pass (ref 40 §6b) › ## Use (at the first release, or any time the repo's face drifts) › ## What it renders › ## Rules (baked in)
- templates/ops/repo-presence/README.template.md (57): # {{NAME}} › ## Features › ## Stack › ## Local development › ## Project layout › ## Deploy & ops › ## Status
- templates/ops/vps-backup/README.md (69): # vps-backup — the deep layer (restic → B2 + Tigris) — LIVE-VERIFIED… › ## Install (agent, on the VPS) › ## Drill findings baked into this template (2026-09-20) › ## A home copy (ref 55 §7 — choices ② and ③) › ## Provider traps › ## What the harness checks (dead-man's switch)
- templates/work-package.md (37): # {{ID}} — {{TITLE}} › ## Objective › ## Owned surface (write only here) › ## Interaction contract (each line is a test) › ## APIs owned › ## Consumes (other packages' interfaces — link to them, never edit th… › ## Constraints › ## Acceptance (definition of done) › ## Stop condition

## §14 TESTS [gen] — what proves what

Run: `python3 -m unittest discover -s skills/deckhand/tests` · `python3 -m pytest skills/deckhand/ops/tests -q` · `node --test skills/deckhand/tryon/test/*.test.mjs` · `python3 -m unittest discover -s scripts -p 'test_*.py'`. All offline (registry HTTP = recorded fixtures; GitHub = a local mock API + file:// remotes).

124 test cases.
- scripts/test_llm_context.py (11) covers llm_context
    18: test_deterministic
    22: test_the_curated_core_resolves
    26: test_index_line_ranges_are_exact
    34: test_an_anchor_renders_its_exact_line
    40: test_every_pointer_lands_on_its_symbol
    49: test_every_module_entry_lands_on_its_definition
    64: test_a_broken_anchor_is_caught_with_its_core_line
    72: test_every_command_and_code_file_is_listed
    82: test_a_code_change_changes_the_map
    90: test_windows_line_endings_do_not_change_the_output
    94: test_check_fails_when_stale_and_passes_after_regenerating
- ops/tests/test_backup_verify.py (3) covers —
    37: test_empty_schedule_list_fails
    42: test_failed_newest_execution_fails
    48: test_all_legs_proven_passes
- ops/tests/test_coolify_api.py (24) covers coolify_api
    27: test_env_beats_config__config_fills__missing_is_loud
    43: test_secrets_file_supplies_token
    51: test_flags_beat_everything
    58: test_build_request_url_headers_params
    65: test_api_parses_json_and_error_text
    74: test_api_json_body_and_content_type
    85: test_wrapped_dict
    90: test_plain_list_and_other_wrappers
    96: test_newest_first_by_created_at
    126: test_transient_empty_then_success_wrapped
    129: test_success_failed_timeout
    134: test_plain_list_shape_still_supported
    139: test_smoke_pass_fail_and_marker
    147: test_smoke_connection_error_is_4
    155: test_cmd_deployments_wrapped_no_crash
    160: test_cmd_deployments_empty
    167: test_envset_accepts_201
    176: test_tunnel_command_shape
    184: test_tunnel_settings_from_env
    192: test_tunnel_settings_unconfigured_is_none
    195: test_ensure_tunnel_spawns_once_and_waits
    212: test_ensure_tunnel_healthy_is_silent
    218: test_wait_skips_stale_finished_then_matches
    242: test_wait_without_expect_commit_unchanged
- ops/tests/test_hostinger_api.py (6) covers hostinger_api
    18: test_base_url_and_auth
    25: test_entries_only_for_targets
    32: test_update_payload_overwrite_and_untouched_records
    53: test_existing_key_no_create
    65: test_missing_key_created_then_attached
    80: test_bare_list_response_shape
- ops/tests/test_repo_presence.py (7) covers —
    26: test_render_full_readme
    44: test_render_drops_empty_sections
    52: test_missing_required_field_fails
    61: test_license_proprietary_with_derived
    69: test_package_json_patch_preserves_everything_else
    81: test_gh_commands_prefill
    88: test_init_end_to_end
- tests/test_autopsy.py (9) covers autopsy, cli, guide, util
    78: test_failures_include_the_ones_a_pipe_hid_and_exclude_answers_and_scratch
    84: test_the_working_recipe_owner_and_rung_come_from_evidence
    97: test_same_session_same_bytes
    107: test_a_known_lesson_that_recurs_climbs_the_ladder
    116: test_playbooks_are_the_actions_that_finished_a_phase_fixes_included
    123: test_apply_writes_lessons_with_recipes_proposals_and_playbooks_never_the_skill
    142: test_dh_run_logs_every_command_and_replays_a_proven_auto_recipe
    158: test_every_dh_call_is_logged_for_the_autopsy
    166: test_a_workflow_confirmed_by_two_sessions_is_shown_by_dh_next
- tests/test_dh.py (21) covers brand, cli, util, verify
    46: test_phases_run_in_order_and_gates_block_in_phased_mode
    69: test_auto_mode_passes_gates_itself
    80: test_example_is_clean
    84: test_every_class_of_dead_end_is_caught
    96: test_protected_pages_need_a_way_in
    104: test_render_and_split_for_parallel_agents
    122: test_ranking_is_deterministic_and_explained
    128: test_blockers_and_personal_bases_first
    136: test_stack_detection
    160: test_check_blocks_then_apply_fixes_identity
    176: test_demo_copy_blocks_but_ai_written_copy_the_owner_saw_labelled_only_warns
    185: test_seed_lessons_match_real_errors_and_run_records_failures
    195: test_from_failure_then_preflight_then_promote
    210: test_suggest_by_shape_and_add_with_github_schedule
    226: test_watchdog_runs_and_reports_down
    238: test_no_domain_preview_passes_smoke_with_a_tls_warning_and_the_handoff_says_preview
    271: test_routes_are_proved_on_the_production_build_which_is_stopped_after
    294: test_secrets_never_enter_the_profile_and_the_vault_is_private
    309: test_project_becomes_a_private_base_with_brand_neutralised
    326: test_next_without_a_run_explains_init
    331: test_errors_are_json_with_codes
- tests/test_library.py (1) covers build, util
    112: test_secrets_block_the_push_then_a_clean_base_lands_private_indexed_syncable_clonable
- tests/test_vet.py (4) covers pool, util
    34: test_a_clean_permissive_web_app_is_accepted
    40: test_every_hard_criterion_refuses_with_its_reason_and_says_what_is_acceptable
    59: test_soft_criteria_warn_but_do_not_refuse
    80: test_refused_is_never_written_and_accepted_is_written_without_raw_evidence
- tryon/test/draft.test.mjs (6) covers draft.mjs, engine.mjs
    49: request -> a brief with the owner content, the project and the rules; nothing is written to source
    66: a gated draft appears labelled AI-generated, carries every owner unit, and keep records provenance, not a licence
    100: the gates reject what an unsupervised model gets wrong — and the page is untouched
    137: asked from an open session: the AI draft joins the licensed variants the owner was comparing
    153: variants opened on the element while the agent was writing are folded in, never wrapped twice
    167: no licensed candidate -> the error says an AI draft is possible, with the exact element
- tryon/test/engine.test.mjs (5) covers engine.mjs, library.mjs
    17: open -> wrapper written once, variants carry the owner text, file parses; discard is byte-exact
    45: show persists the visible variant; keep bakes literal copy, graduates the folder, records the licence
    71: pricing from a .map over literal data: every plan, price and blurb lands in the right card
    85: refusals are typed and write nothing
    95: discard after the owner edited the file unwraps surgically and keeps their edit
- tryon/test/helpers.mjs: helpers
- tryon/test/sections.test.mjs (4) covers engine.mjs, sitelinks.mjs, transplant.mjs
    42: twin card rows are one list: 4 items take the 4-up row, the 2-up row hides whole
    55: a two-tone heading takes the heading in its bright half and the subtitle in its muted half
    67: bake: empty optional half, hidden row and unwired form leave no demo copy and still parse
    101: footer menus come from the plan; social rows keep only the networks the owner is on
- tryon/test/setup-theme-server.test.mjs (6) covers server.mjs, setup.mjs, theme.mjs
    18: palette classes become the site tokens; dark twins drop; hover keeps intent
    28: token layer: only missing tokens, derived from the site colours; strip restores the stylesheet
    40: next.config is wrapped once (ts, mjs, cjs, function config) and still parses
    54: setup -> unsetup is byte-exact and gitignores the state dir
    76: the loader stamps only in development and only project source
    89: proxy injects the overlay into HTML, passes assets through, and gates the API by token
- tryon/test/stamp.test.mjs (5) covers —
    9: stamps host and component elements with their exact position
    18: never touches TS generics, strings, comments or fragments
    36: idempotent, and unparseable input is returned untouched
    43: multi-line tags and jsx in .js files
    49: parseStamp keeps Windows drive colons in the path
- tryon/test/transplant.test.mjs (6) covers transplant.mjs
    46: extract keeps inner markup and in-scope expressions verbatim
    55: parameterize: slots with demo fallbacks, icon kept beside the label, chrome and logos hidden
    68: bind: the owner words land in role order; extra demo buttons hide; demo photos become placeholders
    85: lists: a .map over literal data is unrolled and fills repeated cards in order; extra cards hide
    127: candidate data arrays become list slots merged over the demo items (type-safe cast)
    143: identifier props and object-pattern signatures are both parameterized
- tryon/test/tune.test.mjs (4) covers project.mjs, sitetheme.mjs, tune.mjs
    16: knobs are exact class transforms (variants and ! kept, circles and non-numeric spacing left alone)
    36: a tune session recomputes from the original (no drift), parses, and resets byte-exact
    82: site theme: exact variables, one marked block, fonts rewritten by AST, undo byte-exact
    115: undo restores the stylesheet and layout exactly as they were
- tryon/test/vet.test.mjs (2) covers registry.mjs
    37: a permissive, free, shadcn-schema registry is accepted, indexed and loaded by the catalog
    51: refused with the reason and what is acceptable: copyleft, paywall, no licence evidence, refused list

## §15 CI + HOOKS [gen]

- .github/workflows/ci.yml: Deckhand v2 CI — every suite is offline (registry responses are recorded fixtures). · on: push: pull_request:
    git identity (tests create repos): `git config --global user.email ci@localhost && git config --global user.name ci`
    dh control plane (unittest, stdlib): `python -m unittest discover -s skills/deckhand/tests -v`
    ops scripts (Coolify / backups / Hostinger clients): `python -m pip install -q pytest && python -m pytest skills/deckhand/ops/tests -q`
    try-on engine (node --test, offline fixtures): `node --test skills/deckhand/tryon/test/*.test.mjs`
    version consistency: `python scripts/version_check.py`
    repo scripts (LLM_CONTEXT generator): `python -m unittest discover -s scripts -p "test_*.py" -v`
- .github/workflows/llm-context.yml: Keeps LLM_CONTEXT.md (the cold-start map any AI reads first) in step with main. Branches and pull requests ar… · on: push: branches: [main] workflow_dispatch:
    commit it if it changed: `|`
    every curated anchor resolves: `python scripts/llm_context.py --check`
- .githooks/pre-commit: Regenerates LLM_CONTEXT.md from what is being committed (the git index) and stages it: every commit carries a…
